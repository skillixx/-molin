# 阿里云短信验证码阶段 1 验收报告

## 1. 验收结论

2026-08-03，阶段 1 在本地隔离环境完成测试工程师复核与产品经理业务确认，结论为：**通过本地开发验收**。

本结论最初覆盖短信集成提交 `71018e9`，原始验收来源提交为 `274184b`；相关代码随后通过 PR #314 完成远端检查、正式评审并合并到 `main`，合并提交为 `3aa8f3e`。短信总开关保持关闭，阶段 1 未配置真实阿里云凭证、未向供应商提交请求、未发送真实短信。阶段 1 可以表述为已合并并正式闭环，但仍不能表述为已部署或生产可用。

## 2. 测试环境

| 项目 | 隔离配置 |
|---|---|
| 分支 | `feature/aliyun-sms-template-management` |
| 验收提交 | `71018e9` |
| MySQL | 官方 MySQL 8.0.46，一次性实例，仅监听 `127.0.0.1:23306` |
| Redis | Redis 7.2 RESP3 协议兼容的一次性测试服务，仅监听 `127.0.0.1:26379` |
| API | 当前提交构建的临时二进制，仅监听 `127.0.0.1:28080` |
| 用户控制台 | 当前提交构建的 Vite 服务，仅监听 `127.0.0.1:5275` |
| 短信配置 | `SMS_ENABLED=false`，未注入阿里云凭证 |

测试前核验了端口监听进程与可执行文件路径。机器上既有的 `18080` 服务属于历史临时程序，本次没有使用、修改或停止该进程。

## 3. 自动化与数据库验证

| 验证项 | 结果 |
|---|---|
| `go test ./...` | 通过 |
| `go vet ./...` | 通过 |
| `go mod verify` | 通过 |
| `npm run type-check` | 通过 |
| `npm run test:sms` | 7/7 通过 |
| `npm run lint` | 通过 |
| `npm run build` | 通过 |
| `000058` migration up/down/up | 通过 |
| 全新数据库按顺序执行全部 migration | 通过，共 71 张表，短信表 3 张 |
| `verification_codes.code_hash` 长度 | 64，符合验证码哈希存储要求 |
| 受版本控制文件敏感模式扫描 | 0 命中 |

原始分支验证首次发现 `000020` 重复创建 `idx_audit_operator_id`，会阻断完整 migration；该修复已经包含在最新 `origin/main`。短信集成提交进一步解决了 `000055` DirectMail 与 `000058` 对验证码字段和状态的所有权冲突：`000058` 现在只增加短信专属字段和表，随后从 `000001` 到 `000058` 的全量 migration 复验通过。

## 4. 实际 HTTP 验证

所有请求均命中当前提交构建的本地 API，未使用浏览器拦截或 Mock 返回。

| 场景 | HTTP / 业务码 | 结果 |
|---|---|---|
| 手机注册发码 | `503 / 50300` | 通过，关闭态拒绝 |
| 手机登录发码 | `503 / 50300` | 通过，关闭态拒绝 |
| 手机找回密码发码 | `503 / 50300` | 通过，关闭态拒绝 |
| 换绑手机发码 | `503 / 50300` | 通过，鉴权后关闭态拒绝 |
| 管理员手机二次验证发码 | `503 / 50300` | 通过，鉴权后关闭态拒绝 |
| 邮箱注册、登录、找回密码 | `200 / 0` | 通过，短信关闭不影响邮箱链路 |
| 换绑邮箱、管理员邮箱二次验证 | `200 / 0` | 通过 |

原始隔离 HTTP 复核中邮箱验证码记录 5 条、手机验证码记录 0 条、短信发送日志 0 条，确认关闭态没有生成手机 OTP 或提交外部请求。合入最新 DirectMail 主线后，邮件与短信统一使用 `code_hash` 和 `accepted/failed`，并由全量 Go 测试及全新数据库迁移再次验证两套 Sender 相互独立。

## 5. 浏览器与响应式验收

- 实际 HTTP 与浏览器证据最初在原始短信提交 `274184b` 上采集；移植到最新 `origin/main` 后，相关 handler、错误码和前端交互逻辑保持不变，并重新执行全量 Go 测试、前端类型检查、短信测试、lint、build 以及全新数据库 migration。
- Chromium 桌面视口实际调用发码接口后显示“短信功能当前不可用”，停留在第一步，按钮恢复为“发送验证码”，不启动倒计时。
- Chromium `390×844` 移动视口得到相同关闭态结果；密码输入框数量为 0，页面文档宽度等于视口宽度，无横向溢出。
- 控制台只有预期的接口 `503` 网络记录，无 JavaScript 异常。
- 桌面证据：`output/playwright/sms-phase1-actual-api-closed.png`。
- 移动端证据：`output/playwright/sms-phase1-actual-api-closed-mobile.png`。

## 6. 测试工程师复核

- 阶段 1 功能点、关闭态、错误映射、邮箱回归、数据库约束和响应式页面均已覆盖。
- 原 `QA-SMS-001` 环境串线问题已关闭：实际 API 身份、端口和二进制路径均已核验。
- 原 `QA-SMS-002` 完整手机号展示问题已关闭：失败态不进入成功步骤，成功态已有脱敏单元测试和浏览器回归证据。
- 本轮发现的 migration P1 已修复并完成全量复验。
- 阶段 1 范围内当前为 0 个未关闭 P0/P1 缺陷。

测试工程师结论：**通过**。

## 7. 产品经理确认

- 五个手机验证码业务入口在短信关闭时统一 fail-closed，符合阶段 1 只建设基础能力、不真实发送的范围。
- 邮箱验证码链路保持独立可用，没有因短信开关引入回归。
- 页面错误提示明确，失败后可再次操作，不会误显示已发送、倒计时或密码设置表单。
- 数据库、接口、环境变量、开发说明与测试证据已同步。
- 阶段 2 管理接口、管理页面和真实短信开关均未越界开发。

产品经理结论：**本地业务验收通过**。

## 8. 残余风险与后续门禁

1. `npm audit --omit=dev` 仍报告仓库既有依赖告警（4 个高危、1 个中危），包含 Axios 及构建工具传递依赖；本次短信变更未引入这些依赖。应单独立项升级并回归，不把无关的大版本升级混入阶段 1。
2. Windows 环境未执行 `go test -race`，应由带 CGO/C 工具链的 Linux CI 补跑。
3. PR #314、远端检查、正式评审和合并已经完成；后续阶段仍需继续保持测试、产品和远端 Git 门禁，不能继承阶段 1 的批准直接放行真实发送。
4. 阶段 2 或真实发送前必须重新核验 RAM 最小权限、签名/模板绑定、测试白名单、500 条预算和供应商受理证据。
