# 短信阶段 5 Canary 前 QA 与产品预审

## 1. 预审边界

本预审只覆盖截至 `c0e0d18` 的阶段 5 分支、本地契约、测试服关闭态运行证据和只读门禁。它不触发告警、不发送邮件或
短信、不执行实际应用回滚、不创建生产变更，也不替代测试工程师和产品经理在真实 Canary、生产灰度及观察完成后的最终签署。

当前测试服固定保持：

- `SMS_ENABLED=false`；
- `SMS_TEST_MODE=true`；
- Alertmanager 根路由为 `discard`；
- 活动短信告警和 Alertmanager 活动告警均为 0；
- 修正版通知演练累计完成 1 次 firing 和 1 次 resolved，真实短信发送为 0。

## 2. QA 预审

| 检查项 | 权威证据 | 结论 |
|---|---|---|
| 阶段 5 静态准备度 | `scripts/verify-sms-phase5-readiness.ps1` 输出 `readiness_static=passed` | 通过 |
| 阶段 5 契约回归 | 17 个 `tests/sms/phase5_*.py` 文件共执行 97 项：94 项通过；3 项 Bash 环境依赖用例在 Windows 按原设计跳过，相关固定测试服语法与行为证据另行保留；Canary 计划、脱敏候选生成、观察和回滚执行包装器契约均已显式接入 PR CI | 通过，跳过项不得冒充本机执行 |
| 敏感信息与关闭态 | 166 个文件、424 个历史 blob、11 个暂存对象扫描，0 个发现，短信启用字面量 0 | 通过 |
| 测试服关闭态 | health/ready、固定代理、五模板/绑定、数据库与 Provider 零增量 | 通过 |
| journald 留存 | `8G/50G/14day/1day`、唯一来源、运行时重载均为 true | 通过 |
| Alertmanager 关闭态 | 固定镜像、只读根文件系统、最小权限、回环监听、Prometheus 活跃目标 1 | 通过 |
| 通知演练前置检查 | `notification_drill_preflight=passed`，零活动告警、零通知基线 | 通过，但仍要求另行授权 |
| Alertmanager 邮件投递 | ChangeId `20260805T105517Z` 的 1 次 firing 与 1 次 resolved 均由负责人确认在 QQ 收件箱收到；成功证据契约校验通过 | 通过 |
| Canary 聚合预检 | 六项技术门禁全部为 true，输出 `canary_preflight_ready=true` | 通过，但不构成真实短信授权 |
| 测试服实际回滚 | 最终候选 `20260805T115540Z`、runner SHA-256 `2724b89e...bb46` 仅执行一次、无重试；旧二进制关闭态稳定 10 秒，当前二进制及原进程环境恢复后稳定 10 秒。独立验收确认环境文件未替换、`discard`、零活动告警、Provider/通知/业务 POST/真实短信均为 0 | 通过 |
| 白名单真实短信 Canary | 已选择 `receipt_only` 且不消费 OTP；ChangeId `20260805T132831Z` 脱敏候选以两个低敏别名固定五场景、总量 5、零重试并通过独立校验。当前实服白名单仍只有 1 个号码，尚缺双号码归属/状态、白名单准备、上传预检和真实发送授权 | 计划层通过，运行门禁未通过，阻断最终 QA |
| PR #323 CI | Git head/merge refs 存在；页面检查状态因私有 API 未授权而不可见 | 未验证，阻断最终 QA |
| 生产灰度与 24 小时观察 | 未开始 | 未验证，阻断最终 QA |

QA 预审结论：在已执行的关闭态、邮件演练、实际回滚与只读范围内未发现 P0/P1，通知投递门禁、Canary 技术聚合预检和
测试服恢复运行时验证已经通过；阶段 5 **最终 QA 仍不可签署**。必须补齐真实 Canary、PR CI、生产灰度和观察证据后重新验收。

## 3. 产品经理预审

| 业务规则 | 当前证据 | 结论 |
|---|---|---|
| 未授权不得发送 | 关闭态开关、Provider 零增量、真实短信 0 | 符合 |
| 五场景模板与绑定 | 5 个审核/启用模板、5 个启用绑定、5 个独立模板 | 符合 |
| 白名单和总量限制 | 代码、配置与候选门禁已具备；真实 Canary 未执行 | 设计符合，运行未验收 |
| accepted 不冒充送达 | 页面、接口和文档区分供应商受理与最终收件 | 符合 |
| 告警通知不依赖短信 | 邮件 receiver 已加载；单次 firing/resolved 已实际收件；演练后根路由恢复 `discard`，Provider 与短信均为 0 | 符合 |
| 失败可恢复 | 回滚材料与备份存在；旧二进制关闭态运行及当前二进制/原进程环境恢复均已实际验证，独立验收通过 | 测试服运行态恢复符合 |
| 生产风险控制 | 生产只读基线、审批窗口、开关和观察均未执行 | 未验收 |

产品经理预审结论：关闭态准备、用户提示语义、安全门禁和邮件通知实际到达符合阶段设计；阶段 5 **最终产品确认不可签署**。
真实短信、生产和观察仍属于核心交付行为，不能以候选、HTTP 200、Provider accepted 或本地测试替代；测试服实际回滚已经以运行证据关闭。

## 4. 下一授权顺序

1. Alertmanager 单次 firing/resolved 演练、人工收件确认、`discard` 恢复和零短信复核已完成。
2. 测试服应用实际回滚与恢复演练已按独立授权执行一次并通过独立验收，禁止重复执行。
3. 已选择“真实受理与收件”验收层并生成脱敏候选；下一步通过本地隐藏输入准备两个自有号码，完成归属、未注册/已注册管理员状态和白名单只读预检，再取得固定五次、零重试、明确时窗与观察人的真实短信 Canary 授权。
4. Canary 通过后再申请生产只读基线、关闭态部署、开关与 24 小时观察。
5. 补齐 PR #323 CI、代码审查、独立 QA 和产品最终签署后方可合并。

任何前序门禁未通过时，后序授权不得自动继承。
