# 阿里云短信验证码阶段 5 验收报告

## 1. 当前结论

状态：**阶段 5A 关闭态测试服部署通过，阶段 5 整体尚未验收。**

已完成：主线/工作区审计、测试服只读开关与代理审计、短信供应商指标开发、告警规则、Nginx 静态检查、
Go 全库测试与 vet、两端契约/类型/Lint/生产构建、测试/生产两种回滚 Dry Run、promtool 规则检查、
两端真实浏览器响应式与控制台错误检查，并已把 Nginx、准备度、promtool 和双环境回滚检查加入 CI。
2026-08-05 已在当前 HEAD 再次执行 Go 全库测试/vet 和两端 type-check、Lint、生产构建，全部通过；前端仅有既有
模块类型、动态导入和大 chunk 警告，没有错误。
在提交 `6527919` 后又执行一次同等范围回归：Go 全库测试/vet、两端 type-check/Lint/build、35 项阶段 5 契约和
actionlint 全部通过；阶段 5 契约仅有 Windows 环境按设计跳过的 1 项 Linux Bash 行为用例，测试服 Linux 证据已另行补齐。

2026-08-05 在代码锚点 `5111369` 上完成最新预合并全量回归：Go 全库 `go test ./...` 与 `go vet ./...` 通过；
管理端短信 9、管理员 MFA 8、邮件 11、Vite 隔离 1、出站地址 4 项契约全部通过，并完成 type-check、ESLint 与生产构建；
用户端短信 9、邮件 15、Vite 隔离 1 项契约全部通过，并完成 type-check、ESLint 与生产构建；阶段 5 发布安全契约
41 项通过，另有 2 项 Linux Bash 行为用例按 Windows 环境设计跳过，相关测试服 Linux 行为已分别实跑。准备度 SelfTest、
Nginx 来源头检查和差异检查通过。两端构建只有既有模块类型、动态导入、PURE 注解和大 chunk 警告，没有错误。
本机没有 Docker 与 actionlint，因此本轮没有复跑容器 promtool、Linux `-race` 或 actionlint；这些仍必须以 PR CI
实际结果为准，不能由本地普通测试替代。

新增完成：阶段 5 分支提交并同步主线、部署前备份、测试服固定代理 `/28`、可信代理配置、新后端关闭态部署、
4 条短信告警加载，以及双代理 `503/50300`、数据库/指标/日志零发送增量核验。
部署后只读验收脚本已增强为严格失败关闭，并完成正确哈希正例、错误哈希反例和 30 秒零发送观察。
2026-08-05 复跑同一 30 秒关闭态观察仍通过：API 进程稳定，短信保持关闭，发送摘要保持 `13:13:0`，Provider
调用总数保持 0，固定代理、Prometheus 和数据库摘要均无漂移；本次没有业务配置修改、服务重启或短信发送操作。
测试服管理代理上的 5 个只读短信管理 GET 接口也已完成实服核验：授权管理员请求全部成功，模板与绑定均为
`5:5:5`、发送日志为 `13:13:0`，响应未发现敏感字段或完整手机号。无权限账号返回 `403/40003`，权限满足但
双重认证失效的账号返回 `403/40031`；两组请求前后短信日志、验证码与审计总数保持 `13:1545:251`，Provider
调用指标保持 0。短期 JWT 仅在内存中生成和使用，没有输出或持久化，也没有调用 4 个短信管理写接口。
随后使用真实 Chrome 对测试服管理页面完成认证后只读复验：概览 `5:5:5:5/5:0`、模板 5、场景 5、
发送日志 13；`1440/1024/768/390` 四档均无横向溢出。页面期间短信管理请求只有 5 次 GET，写请求与非 200
响应均为 0，功能性控制台错误和页面异常均为 0，完整手机号与 DOM 内 Token 均为 0。另观察到 1 条未能关联
具体响应路径的静态资源 404 控制台提示，不影响页面和 API，本阶段作为低优先级环境观察保留。
测试服回滚材料与通知链只读预检也已固化并执行：冻结备份点 11 个文件、外部固定的 `SHA256SUMS` 摘要、
属主/权限、新旧 API 哈希、当前运行 PID/监听和 x86-64 架构均通过，三份容器快照结构有效且引用镜像仍存在，
当前 health/ready 为 `200/200`。进一步检查发现旧环境语法、基础键和短信关闭态有效，但缺少当前固定代理信任，
且仍含两项已废弃的 `SMS_TEMPLATE_CODE_*` 键，因此禁止整份恢复，静态前置状态为
`backup_env_missing_fixed_proxy_trust`。这些证据只证明恢复材料完整及当前版本健康，不证明旧版本能够成功启动。另一方面，
Prometheus 已加载 4 条短信规则。2026-08-05 完成 Alertmanager 邮件候选关闭态部署后，实际配置引用、容器和进程均为
1，Alertmanager/Prometheus health/ready 均为 `200/200`。修正版邮件演练 `20260805T105517Z` 随后仅执行 1 次 firing
和 1 次 resolved，负责人均确认在 QQ 收件箱收到；结束后根路由恢复 `discard`，活动告警 0，通知/请求累计 3/3、失败 0，
Provider 与真实短信增量为 0。成功证据契约及完整 Canary 聚合预检均通过，但这不构成真实短信授权。
安全候选生成器已通过本地行为契约和默认无连接 SelfTest。项目负责人随后使用精确批准口令授权真实生成，测试服已排他创建
`candidate-20260805T015043Z.env`；候选文件 SHA-256 为 `8435f846ff2e5815bec889ac4e4c32d432acb06bb05c0e1e9c3bd6b02bb65494`，
目录/文件属主权限为 `pc:700`/`pc:600`。独立只读解析确认短信关闭、测试模式开启、固定代理精确保留、废弃模板键和重复键均为 0，
未输出敏感值。该核验现已固化为按 UTC ChangeId 执行的只读 CLI，并在测试服 Linux 通过有效候选及缺失、符号链接、错误权限、
短信开启、代理漂移、废弃键、重复键八类行为自测；真实候选经新 CLI 复验得到同一 SHA-256。生成过程未替换当前环境、未重启服务、
未发送短信；候选安全性通过不等于旧二进制运行时回滚通过。旧二进制运行/当前版本恢复执行器随后完成本地冻结和只读预检：
首个 `20260805T112823Z` 因前端端口冻结错误而在只读阶段失效，未上传、未重启；修正版 `20260805T113149Z` runner
SHA-256 为 `347b59734d87d5a611b47627044822bc4e103c250262bcfa814faecd7819105a`，远端流式 `bash -n`、SelfTest 和关闭态
只读预检通过。该候选随后由增加写入前预检和运行时候选快照的版本取代，最终敏感快照清理加固候选为
`20260805T115540Z`，runner SHA-256 为
`2724b89ea0096b15e5c443a2f5dfdd7e80f93c971ff2fb22a3585a5a1ad2bb46`；该版本还证明当前环境文件、关闭态候选和运行进程
环境一致，相同三类校验再次通过。项目负责人随后独立批准远端暂存与只读预检；包装器以固定 ED25519 身份排他创建
`/home/pc/molin/rollback/sms-phase5/runtime-drill-staging/20260805T115540Z`，上传唯一 runner 并固定为 `pc:600`。
远端 SHA-256、`bash -n`、SelfTest 和 `--preflight` 全部通过，关闭态仍为 `SMS_ENABLED=false`、`SMS_TEST_MODE=true`、
Alertmanager `discard`、活动告警 `0:0`。暂存窗口服务重启、通知 POST、业务 POST 和真实短信均为 0。项目负责人随后以
ChangeId、runner SHA-256、停止/启动次数、信号边界和失败恢复范围进行独立授权；实际执行包装器只调用一次冻结 runner，
得到 `execution_attempts=1`、`execution_retries=0`。旧二进制使用关闭态候选稳定运行 10 秒后，当前二进制及原进程环境自动恢复并
稳定运行 10 秒；独立只读验收确认 `old_binary_runtime_verified=true`、`current_binary_restored=true`、
`current_environment_unchanged=true`、`rollback_restore_runtime_verified=true`。实际窗口服务停止 2 次、启动 2 次，
`infra/.env.test` 未替换，notification/business POST、邮件、Provider 调用和真实短信增量均为 0。
测试服日志留存只读审计与受控部署均已完成：journald 正常运行、持久目录存在，`SystemMaxUse=8G`、
`SystemKeepFree=50G`、`MaxRetentionSec=14day`、`MaxFileSec=1day` 已由独立只读入口确认生效，
`log_retention_policy_verified=true`。部署过程保持短信关闭态并具备失败自动回滚；SSH 登录可能增加访问审计日志，
Provider 与真实短信发送均为 0，不能据此证明用户侧收件事实。完整分支 Standards/Spec 双轴审查发现的部署回滚、关闭态验收、白名单可重复核验、
部署探针 Redis 副作用和证据口径问题均已修复，复核未发现新的 P1/P2。原 P3 重复 SSH 指纹校验也已收敛为共享函数，
六个远端包装器通过同一固定目标与 ED25519 指纹契约。Canary 前 QA/产品预审已记录在
`docs/sms-phase5-pre-canary-review.md`：已执行关闭态、邮件演练和实际回滚范围未发现 P0/P1，通知、技术聚合门禁及测试服
回滚恢复运行时验证已通过；真实 Canary、PR CI、生产和观察证据仍缺失，最终 QA 与产品确认均不可签署。

未完成：测试服 24 小时观察、最终状态与五窗口证据组装、PR #323 最新修复提交及 CI、
生产只读审计、生产部署、生产 Canary、生产开关、独立 QA、产品经理批准和合并。

2026-08-05 已按项目负责人既有授权将 `codex/aliyun-sms-phase5-canary-release` 推送至 `origin`。以
`73753ea267a16dad7d297345e9594f964cae9305` 为审计锚点时，Git 远端同时暴露 `refs/pull/323/head` 指向该提交，以及
`refs/pull/323/merge`，其第一父为 `main@60b569f5048f8c3e781f51052b4b853c20c7c4f7`、第二父为阶段分支 head。
2026-08-06 已通过 `git fetch --prune origin` 与 GitHub CLI 刷新远端状态：PR #323 仍为 OPEN、非草稿且
`MERGEABLE`，远端阶段分支仍为 `a102bf6258d0fd9412503a562ea0943cb936d08f`。该提交的 6 个 CI job 中 5 个成功，
“阶段 5 短信发布安全门禁”失败；日志确认根因是 Ubuntu runner 不存在硬编码的 `powershell.exe`。本地已改为复用当前
PowerShell 主机路径后，提交 `a99c323` 已推送；其 CI 中 readiness 与敏感门禁均通过，但下一组契约暴露两个候选脚本把
`$isWindows` 与 PowerShell Core 只读内置变量混用。本地已统一改为 `$isWindowsPlatform`，CI 同组 15 个契约入口全部通过；
正式 PR CI 仍须以该后续修复推送后的结果为准。

## 2. 当前门禁

本节是阶段 5 当前状态的唯一权威摘要。其他阶段 5 文档分别保存部署、观察、代理、安全和回滚证据，不另行定义
冲突的总状态；状态变化必须先更新本表，再同步 README 的简要进度。

| 门禁 | 状态 |
|---|---|
| 阶段 4 已进入 main | 通过 |
| 本地短信指标专项测试 | 通过 |
| 仓库 Nginx 静态校验 | 通过 |
| 回滚 Dry Run | 通过 |
| 本地真实浏览器验收 | 通过：管理端短信页和用户端注册页四档无横向溢出，登录/找回密码手机端无横向溢出 |
| 测试服开关安全状态 | 通过：false/true |
| 测试服数据库与五绑定 | schema `64:0`；5 个审核/启用模板、5 个启用绑定、5 个独立模板 |
| 测试服端到端可信代理 | 关闭态通过：专网 `/28`、固定 `.2/.3`、可信配置、伪造转发头覆盖均已核验 |
| Prometheus 官方规则检查 | 通过：同版本 promtool 校验；运行实例加载 4 条短信告警，目标 up，40 个调用序列可查询 |
| 告警通知链 | 通过：修正版演练仅 1 次 firing 和 1 次 resolved，均人工确认 QQ 收件；结束恢复 `discard`，活动告警 0、通知失败 0、Provider 与真实短信增量 0；成功证据契约通过 |
| 阶段 5 分支同步 | 2026-08-06 已执行 `git fetch --prune origin`；当时阶段分支相对 `origin/main@60b569f5048f8c3e781f51052b4b853c20c7c4f7` 落后 0、领先 91。随后 36 个修正版生成器、测试、CI 和文档路径已作为 `a99c323` 提交并推送；后续 CI 平台兼容修复另行收口，不改写该审计锚点 |
| 阶段 5 CI 安全门禁 | CI 已接入部分失败只读诊断、启用态只读诊断、旧键清理、事后核验、五窗口快照、观察进度、最终状态与观察组装契约；当前本地 141 项阶段 5 契约通过、3 项按环境门禁跳过，敏感扫描覆盖 191 个文件及 534 个历史 blob，发现 0、短信开启字面量 0。`a99c323` 的远端 readiness 与敏感门禁已通过；后续契约发现 `$isWindows` 与 PowerShell Core 内置变量冲突，本地已改名并通过 CI 同组 15 个入口。正式 PR CI 仍须等待修复推送后的完整结果 |
| 测试服 Canary 聚合预检 | 通过：关闭态、回滚候选、回滚材料、监控、通知演练和 journald 留存全部为 true，输出 `canary_preflight_ready=true`；只表示技术前置满足，不构成真实短信授权 |
| 测试服关闭态部署 | 通过：新二进制 hash 已核验，health/ready 通过，双代理发码均 `503/50300` |
| 部署后可重复核验 | 通过：精确网络/IP、数据库、指标、告警、抓取目标和观察增量均纳入脚本断言 |
| 测试服实际回滚 | 通过：最终候选 `20260805T115540Z`、runner SHA-256 `2724b89e...bb46` 仅执行一次且无重试；旧二进制关闭态稳定 10 秒，随后当前二进制及原进程环境自动恢复并稳定 10 秒。独立只读验收确认当前环境文件未替换、关闭态与 `discard` 保持、活动告警 `0:0`、发送摘要 `13:13:0`、Provider 0；服务停止/启动各 2 次，通知 POST、业务 POST、邮件和真实短信均为 0 |
| 测试服日志留存策略 | 通过：`8G/50G/14day/1day` 已由有权限运维在固定测试服受控部署；独立只读验证得到 `log_retention_configuration_complete=true`、`log_retention_runtime_reload_verified=true`、`log_retention_policy_verified=true`，真实短信和 Provider 增量为 0 |
| 测试服短信管理只读 API | 通过：5 个 GET 经管理代理实服返回正确；未登录 `401/40001`、无权限 `403/40003`、MFA 失效 `403/40031`；响应脱敏且数据库/审计/Provider 零增量 |
| 管理后台认证后真实数据显示 | 通过：真实 Chrome 展示 5 个模板、5 个场景、13 条日志；四档无横向溢出，短信写请求 0；保留 1 条无路径静态 404 环境观察 |
| 五个真实发码场景计划 | 产品负责人已选择 `receipt_only`，不消费 OTP、不批准业务状态变化。供应商频控修正版 ChangeId `20260806T053735Z` 已完成五场景各一次、两个 65 秒间隔和零重试，关闭态恢复成功；两个自有号码的五场景均已人工确认收件，修正版事后只读核验已确认五次供应商受理和五条 OTP 未消费 |
| 真实短信 | 测试服 Canary 通过：持久数据库确认五场景各 1 条 accepted、供应商受理字段完整、5 条 OTP 全部未消费；项目负责人已逐场景人工确认两个自有号码全部收件。该结论仅覆盖本次测试服 `receipt_only` 组合，不代表生产发布或 OTP 消费业务验收 |
| 生产发布与观察 | 未开始 |
| P0/P1 | 当前本地开发未发现；最终以 QA 结论为准 |

本地最新验证明细：阶段 5 契约共执行 141 项并全部通过，3 项 Bash 环境依赖用例在 Windows 按设计跳过；最新敏感扫描覆盖 190 个文件和 534 个历史 blob，发现 0、短信启用字面量 0。新增契约覆盖部分失败只读诊断、启用态只读诊断、旧键精确清理、事后只读核验、固定 SSH 身份、失败自动回滚、默认关闭和零发送；五档观察快照、连续前缀进度、最终状态、权威验证与离线组装覆盖 5m/15m/30m/2h/24h、输入摘要和人工收件确认。相关候选和观察契约已接入 PR CI。本机仍没有 actionlint，因此工作流最终语法和执行结果必须以 PR CI 为准。管理端短信 9/9、管理员 MFA 8/8、邮件 11/11、Vite 隔离 1/1、出站 URL 4/4；
用户端短信 9/9、邮件 15/15、Vite 隔离 1/1；两端 type-check、ESLint、build 通过。前端依赖审计仍有
1 个 moderate、6 个 high 的既有风险，本阶段未执行破坏性依赖升级。

浏览器验证使用 Chrome/Playwright，覆盖 `1440×900`、`1024×768`、`768×900`、`390×844`。管理端以
本地脱敏模拟响应检查短信管理页，未使用真实 Token、手机号或验证码；用户端只打开注册、登录、找回密码页面，
未点击发送验证码。所有检查均未发现横向溢出，最终控制台错误为 0。浏览器验证结束后仅关闭本轮启动的
5173/5176 服务，既有 5174 进程保持不变。

测试服认证后浏览器复验使用现有合格管理员身份生成 10 分钟内存 Token，只在隔离浏览器上下文的
`access_token` 存储中短暂存在；Token 不进入命令参数、输出、截图、DOM 或文件，浏览器上下文结束后即销毁。
该次只浏览概览、模板、场景和日志页签，没有点击同步、状态开关、保存配置或测试发送。

本机 Windows Go 未启用 CGO，直接执行 `go test -race` 会被 Go 工具链拒绝；仓库 Linux CI 已配置 MySQL 8、
Redis 7 和 `go test -v -race -count=1 ./...`，并新增阶段 5 发布安全门禁。该竞态与隔离依赖证据必须等 PR CI
实际成功后才能记为通过，当前不以普通 `go test` 替代。

## 3. Goal 状态

Goal 必须保持 active。只有真实 Canary、生产灰度、生产开关与观察、QA/产品/审查、PR/CI/合并和 `origin/main`
核验全部关闭后，才允许标记 complete。

## 4. target-admin 白名单候选补充证据

本地生成与静态验证通过后，最终 ChangeId `20260805T180909Z`、runner SHA-256 `d202e6f7...0079f7` 获得一次性精确执行授权。候选只从 target-new 单项白名单新增 target-admin，保持短信关闭和测试模式，并冻结固定 SSH 身份、变更前全门禁、stdin SQL、不可中断原子目录排他锁、同名证据隔离、环境/进程快照、10 秒稳定观察与失败自动回滚。

实际执行成功且未重试：白名单数量 1→2、target-new 保留、target-admin 新增，`SMS_ENABLED=false`、`SMS_TEST_MODE=true`、活动告警 `0:0`；发送日志、Provider 与通知计数零增量，服务停止/启动各 1 次，业务 POST、上传、短信提交请求和真实短信均为 0，远端 stderr 为空，未触发自动回滚。该执行证据来自获批 runner 的低敏结果，并已由后续独立只读复核确认。

新的变更后只读复核候选已本地生成：ChangeId `20260805T182328Z`、计划 SHA-256 `f84c96a6...ccda89`、runner SHA-256 `2a4225f6...a8540f`。语法、只读 SQL、双目标白名单总门禁、默认关闭和敏感字面量检查通过。

该 runner 随后按一次性授权执行，固定 SSH stdin 仅连接 1 次且未重试。`target_state_readonly_preflight=passed`、`readonly_exit_code=0`；关闭态、测试模式、账号/IAM 和双目标白名单均通过，发送日志零增量，配置修改、业务 POST、上传、短信提交请求、敏感值持久化和真实短信均为 0，远端 stderr 为空。因此白名单门禁正式通过；本结果仍不构成真实短信授权、供应商受理或手机收件证明。

五场景真实收件默认关闭候选也已完成本地生成与静态验证：ChangeId `20260805T191326Z`、计划 SHA-256 `772b5bcd...80a3b`、runner SHA-256 `062f45cd...e543c`。候选固定五场景各一次、总量 5、零重试，并在任何发送前只读绑定管理员 Token、target-admin、直接 admin 与 `user:manage`；成功或失败路径恢复关闭态，恢复失败则保留受控材料和锁供人工处置。

2026-08-06，该候选按完整摘要和精确执行边界取得一次性授权。启动前本地摘要、默认关闭与 SelfTest 复核通过；可见交互进程仅启动一次且已经结束，未自动重试。项目负责人确认管理员 Token、手机号及身份绑定前置验证成功，但尚未提供五场景提交、供应商受理、恢复关闭态、运行增量和逐场景人工收件的低敏摘要；候选目录也没有持久结果文件。因此本次授权已消耗且禁止重跑，当前仅能记录“前置身份门禁通过、执行结果证据待回收”。执行结束后的本地准备度、敏感扫描和 115 项阶段 5 契约复跑通过，3 项按 Windows 环境设计跳过；这些本地证据不替代缺失的远端运行与人工收件证据。

同日，项目负责人提供管理员安全认证页面截图，页面对一次手机发码操作显示“短信功能当前不可用”，未进入下一步或显示成功倒计时。源码契约表明该提示对应 HTTP `503` / 业务码 `50300` 的失败关闭响应；它与短信恢复关闭态的预期一致，但同一错误也可能由 Sender/配置、白名单、模板或 Redis 门禁不可用引起，不能单独作为服务端 `SMS_ENABLED=false`、Provider 零调用或五场景执行结果的权威证明。该页面操作不属于已批准五场景，后续证据核算必须与 Canary 提交分离。

为恢复缺失的服务端低敏证据，已在仓库外生成并按精确授权执行事后只读候选 ChangeId `20260805T193505Z`，绑定源执行 `20260805T191326Z`，runner SHA-256 `57f3233d8d3f08b302173935c0cbb21c3bbf33e7677be09da0df991e11b0dae3`。runner 仅建立 1 次固定 SSH stdin 连接且未重试，结果为 `canary_postcheck=blocked`、退出码 3：关闭态、测试模式、health/ready、白名单数量 2 与恢复锁清除均通过，但短信日志仍为基线 `13:13:0:0`，不存在基线后五场景日志、供应商受理字段或关联 OTP，故确认源执行没有发送。核验自身没有业务 POST、配置修改、上传、服务信号、自动重试、邮件或短信。

源执行零发送的确定性根因是发送前 Alertmanager 门禁引用旧路径 `/home/pc/molin/infra/alertmanager/alertmanager.yml`，而实际部署关闭态配置为 `/home/pc/molin-alertmanager-phase5/20260805T084215Z/alertmanager.closed.yml`，所以在远程账号/IAM 绑定、加锁、打开短信开关及发送前已经退出。事后候选还误用 `INTERNAL_METRICS_TOKEN`，实际键为 `INTERNAL_API_TOKEN`，因此其中 Provider 当前进程指标 unavailable 不能解释为零；`alertmanager_route_discard=false` 也属于同一路径缺陷。仓库内生成器已本地修正为核验实际配置、容器运行态和 ready，并为后续 runner 增加严格低敏结果持久化；阶段 5 契约 115 项通过、3 项环境跳过，静态准备度与敏感扫描通过，修正过程没有联网或真实发送。新的真实 Canary 仍需新 ChangeId 和独立精确授权。

修正版最终候选已在本地离线生成：ChangeId `20260805T200244Z`、receipt-only 计划 SHA-256 `3d47f96d172f3fc976b5acdc20e65b64813669bbce64799b0abc9d587fa45045`、runner SHA-256 `d7748b2df0056b9fcd2775b464a0dafd622142809f30d723deeff4d8de96c9ec`。候选固定五场景各一次、总量 5、零重试、失败自动恢复关闭态，修正 Alertmanager 实际部署路径并核验容器/ready；运行摘要仅以预定义低敏字段白名单单次创建，不保存手机号、Token、OTP 或 stderr。计划、摘要、PowerShell、UTF-8 无 BOM Bash、默认关闭、双层 SelfTest、敏感字面量和全量 115 项阶段 5 契约均通过，3 项环境跳过；网络、上传、测试服变更和真实短信均为 0。更早的 `20260805T200101Z` 因结果字段名白名单不够严格已在执行前隔离，不得使用。最终候选尚未获批或执行。

该最终候选随后取得一次性精确授权并执行，授权已经消费且禁止重跑。低敏结果 SHA-256 `ea58e017b7a47f48efaf1ed1e670b43ce6f0eec7c65afb727eb0294c8b00524f` 显示 `canary_send=blocked`、`failure_gate=enabled_api_ready`、退出码 2：临时启用后的 API 没有在门禁时间内 ready，runner 在业务提交前停止，`sms_submission_requests=0`、自动重试 0、真实短信 0。失败路径已自动恢复关闭态，服务停止 1 次、启动 2 次，远端 stderr 为空。当前证据不能区分启用进程退出、身份/环境门禁或 ready HTTP 失败；下一步必须先获得新的关闭态只读诊断授权，禁止直接生成或执行第三次真实发送候选。

关闭态只读诊断候选已使用新 ChangeId `20260806T015216Z` 离线生成，runner SHA-256 为 `65e1aed60921cea057bbe63fbaf663bb705171f31fffcbc9ec48025db356c9f6`。候选只输出启用配置就绪所需的预定义布尔值，不输出配置原值；本地 PowerShell/Bash 语法、默认关闭、自测、契约、readiness 和敏感扫描均通过，生成过程外部动作和真实短信为 0。该候选尚未取得测试服执行授权，因此当前阶段结论仍是“真实 Canary 未通过，根因待只读诊断”，不得提升为阶段 5 验收通过。

该诊断随后按一次性授权执行，结果 SHA-256 `6326a849d654e8cc21dfc0285850d74b85a4d92c628ec65834c98690402528ea`。当前关闭态 API、环境文件、二进制、Aliyun 新配置字段和白名单均通过，文件与进程配置一致；唯一失败门禁是 `legacy_sms_keys_absent=false`。因此已确认启用态启动失败由旧短信环境键残留触发，而不是新 Aliyun 必需字段、HMAC、Endpoint、白名单或当前 ready 异常。诊断没有配置修改、服务操作、业务 POST、邮件或短信。阶段 5 仍未通过；必须先以新 ChangeId 完成旧键精确清理和自动回滚演练，再重新进行关闭态复核，最后才可申请新的真实 Canary 授权。

旧键精确清理与自动回滚候选已使用 ChangeId `20260806T021613Z` 离线生成，runner SHA-256 为 `6979bf61a6d4352e9adb8d7540b335bd04fffb710e1435749f985990f4882117`。候选仅删除三个旧键，保留全部 Aliyun 新配置和关闭态；具备文件/进程双备份、排他锁、失败自动恢复、同 PID 信号门禁、Alertmanager discard 与 10 秒稳定性核验。121 项阶段 5 契约通过、3 项跳过，readiness 和敏感扫描通过；生成与验证期间外部动作和真实短信为 0。候选尚未执行，因此阶段结论仍为“根因已确认、修复候选就绪、测试服配置未变更”，不得记为 Canary 或阶段 5 验收通过。

该候选随后按一次性授权执行成功，结果 SHA-256 `3564bd9259f819b4386e4867a5666118173c2c52b8754ff7833f3e28194a366d`。测试服文件和运行进程中的三个旧键均已移除，Aliyun 新配置与关闭态保持一致，API ready 并稳定 10 秒，Alertmanager 保持 discard；成功路径服务停止/启动各 1 次，没有回滚失败、残留恢复材料或排他锁。业务 POST、邮件、短信提交和真实短信为 0。清理修复本身通过，但阶段 5 仍未通过：需先执行新 ChangeId `20260806T022804Z` 的关闭态只读复核，再评估是否具备重新申请真实 Canary 的条件。

清理后只读复核 ChangeId `20260806T022804Z` 随后通过，结果 SHA-256 `eb666eb3520bac38bbfeafe8778a963b448b951c946d2c825043c67648889a43`。旧键不存在、全部 Aliyun 启用配置有效、文件/进程一致、关闭态 API ready，远端 stderr 为空；只读执行没有任何配置或服务变化，也没有业务 POST、邮件或短信。根因修复门禁据此关闭。

五场景真实收件候选 ChangeId `20260806T040627Z` 已按一次性授权执行。结果 SHA-256 `51eb9596cdbe01fa2c60959495ac464431f359772284e5dd48942bf53898ec4d`：`register`、`login` 提交标志为 true，第三个 `reset_password` 失败，后续 `bind_phone`、`admin_verify` 未执行；业务提交 3、自动重试 0、退出码 2，失败后自动恢复关闭态，服务停止/启动各 2 次，远端 stderr 为空。原授权和 ChangeId 均已消费，禁止重试。该结果不含供应商受理、发送日志增量或收件证据，因此实际外发/费用暂按 0–2 次未知处理，阶段 5 尚未通过。

源码只读审计排除了应用 IP 限额、跨场景 Redis 门禁和重置场景账号状态前置拒绝。随后 ChangeId `20260806T051625Z` 的一次性只读诊断执行通过，结果 SHA-256 `d8006dffbd18fe631ee5c652cd7f794472aefb4a34db619e42f9eb5ba86f638c`：日志总数 13→16，事件 accepted 2、failed 1；`register`、`login` 各 accepted 1，`reset_password` failed 1 且精确归类为供应商频率限制；3 条 OTP 全部未消费。关闭态、ready、Alertmanager discard 和恢复清理均通过；诊断自身无配置、服务、业务 POST、邮件或短信副作用。根因据此确认。

修正版计划与 runner 已使用新 ChangeId `20260806T053735Z` 离线生成。计划 SHA-256 `2511648d46c6ef3395d6a8fab0e9400e400c2fe66bf0dde184f799a61efca625`，runner SHA-256 `39b4a84b8e4b9b009cfb05045fd859d5d889405ea44e18dada3139057dd5b7aa`。计划强制两个 65 秒等待，负载分别在管理员号码的连续场景之间应用，同时使新号码的两场景间隔超过一分钟；等待期间进程存活和结束后 ready 均受控核验。五场景各一次、提交上限 5、自动重试 0、失败自动恢复关闭态不变。计划、PowerShell、默认关闭、自测、Git Bash 语法和精确调用/等待计数通过，结果文件不存在；本地生成没有联网或发送，执行仍需新的精确授权。

该修正版随后按一次性精确授权执行成功。结果 SHA-256 `0ae03e57b796993f7b5418891720ea62601b9f6bd2a47605b7e06c2388cc29d9`：五场景提交标志全部为 true，提交请求 5、完成场景 5、自动重试 0，两个 65 秒窗口均完成；发送前日志游标 16、验证码游标 1751，完成时间 `2026-08-06T05:57:50Z`。结束恢复 `SMS_ENABLED=false`、`SMS_TEST_MODE=true`，服务停止/启动各 2 次，远端 stderr 为空。该结果尚不替代供应商受理、OTP 未消费、监控和人工收件证据。

事后只读候选 ChangeId `20260806T060018Z` 已离线生成并绑定源计划 SHA `2511648...ca625`、源 runner SHA `39b4a84b...5b7aa`、源结果 SHA `0ae03e57...c29d9`；runner SHA-256 `d547cd54e2d8d3ee917fa4c4716dc13cbb03c9d9047909184ed662706be5581d`。默认关闭、PowerShell、SelfTest、Git Bash 语法和只读/零副作用检查通过，尚未取得测试服执行授权。

项目负责人已人工确认五场景全部实际收件。低敏收件确认文件 SHA-256 `6c9bda7862084567b78521921234e2c36afa0e143aa4b4dbc6ece2d19af0d61c`，仅保存源 ChangeId、UTC 确认时间和五个场景布尔值，不保存手机号或 OTP。

原事后候选随后单次执行，在 `provider_metrics_shape` 阻断，结果 SHA-256 `c9c56de2a0d0c368bd24244d14ea48f1146f7a5906487bc0d444da8c1a9b4d75`。此前数据库硬门禁已确认五条发送日志、五条 accepted、五场景各一、供应商受理字段完整、五条验证码全部未消费且日志关联完整；失败仅因恢复关闭态重启 API 后当前进程指标归零，而候选错误要求当前进程计数至少 5。执行无副作用，旧 ChangeId 已消费。

修正版事后候选 ChangeId `20260806T062059Z` 已按 runner SHA-256 `1c0291b2d3eb07b872a0aeae24bebf42e04c1f3a342fa844fc3b4eb67b3ca383` 的一次性只读授权执行通过，结果 SHA-256 `5fd533d891772e57675721463f4c94f8f9952bce81ec05410908d81dc7ee421e`。基线后五条日志、五条 accepted、五个独立场景、五条完整供应商受理字段、五条未消费 OTP 及日志/验证码关联全部通过；关闭态、health/ready、双白名单、Alertmanager discard、零活动告警、零通知失败和恢复锁/材料清除均通过。恢复重启后当前进程 Provider 计数可读且为 0，不替代持久数据库的五次受理证据。本次只读核验配置修改、服务操作、业务 POST、邮件和短信均为 0。测试服真实收件 Canary 门禁因此通过，但五档观察、PR/CI、生产与 QA/产品最终验收仍未完成。

观察快照 ChangeId `20260806T060345Z` 的 5m、15m、30m、2h 四个“最小经过时间”窗口已按 runner SHA-256 `9b89eab7bde8461d3002422f58672ce4adf2b318e0c4c961423d8f6139faa636` 各执行一次并通过，实际经过时间分别为 1812、1821、1830、13965 秒。四次均确认 health/ready 200、累计发送 `21/20/1` 保持不变、当前进程 Provider 非受理 0、活动告警 0、通知失败增量 0，且每次只建立一次固定 SSH 连接、零配置/服务/业务/消息副作用。2h 快照完整 SHA-256 为 `1c14fed55aba8fdbb5b125baf30c69826c988a180d475de37b30a75c6feb3cc6`。仅余 24h 尚未执行，五档观察门禁仍未闭环。

上述四份真实快照随后通过纯离线连续前缀验证器复核：源 Canary 和四份快照完整摘要均匹配，窗口顺序/时间、发送 `21/20/1`、当前进程 Provider `0/0`、health/ready、活动告警与通知失败全部通过，输出 `phase5_observation_progress=passed`、`through_window=2h`、`snapshots_verified=4`。该复核没有联网或发送消息。

完成前三个窗口后的工具链审计发现最终观察组装还缺少 `final_state` 的受控生产路径。仓库现已补充纯离线最终状态组装器，强制校验源 Canary、修正版事后核验和 24h 快照三项完整摘要及输入字段白名单，并以基线加 5 的精确累计发送、不消费 OTP、关闭态、discard、零告警/通知失败作为输出门禁；关闭态新增发送、输入篡改、未批准敏感字段或异常混合换行均失败且不创建文件。5 项契约通过，资产已纳入 readiness 和 PR CI。该修复没有连接测试服或发送消息；实际最终状态只能在 24h 快照通过后生成。

最终组装预审同时修正了 Provider 指标跨重启语义：发送日志是持久证据，关闭态五窗口必须精确保持基线加 5；Provider 指标是当前进程计数，Canary 恢复重启后允许从 0 开始，但五窗口不得增长且非受理不得超过调用数。生产启用态仍保留相对基线增量与发送日志一致的强断言。该修正与修正版事后核验的既有结论一致，相关正反例和离线组装契约均通过。

五档观察快照候选 ChangeId `20260806T060345Z`、runner SHA-256 `9b89eab7bde8461d3002422f58672ce4adf2b318e0c4c961423d8f6139faa636` 已绑定源成功结果与完成时间生成，固定 5m/15m/30m/2h/24h 五个只读窗口，不自动等待、不重试、每窗口只写一次结果。5m、15m、30m、2h 已各执行一次并通过；仅余 24h 仍待独立授权和最小时间门禁。

仓库已补齐与上述两项游标绑定的独立事后只读核验生成器。生成门禁要求源计划、源 runner 和源成功低敏结果三项摘要全部匹配；修正版候选 `20260806T062059Z` 已一次执行通过，确认五场景日志、供应商受理字段、OTP 未消费、关闭态、内部指标可读、Alertmanager discard、零活动告警/通知失败及恢复锁清除，执行自身没有配置或服务操作。

五档观察证据离线组装器已补齐：它只接受工作区外、摘要冻结的源成功结果、逐场景人工收件确认、五个观察快照与最终状态，随后复用权威验证器生成新证据文件。真实发送、五场景人工收件及前四个快照已经形成证据；仍缺 24h 与由 24h 快照派生的最终状态，所以当前不能生成完整观察证据。

五窗口固定测试服只读快照生成器也已补齐：它绑定源成功结果和完成时间，以同一冻结 runner 在每个窗口最多执行一次，不 sleep、不重试，每次仅允许固定 SSH 单连接和只读查询，输出可直接供离线组装器使用的低敏 JSON。真实 runner 已生成，前四个窗口已连接测试服各一次并通过；仅余 24h 尚未执行。

随后收到的一次批准误用了修正前旧摘要 `23112fea...36b1e`，摘要门禁在 SSH 连接前拒绝执行；外部连接、读取、上传、业务 POST、配置修改、服务信号、邮件和短信均为 0。该批准不构成当前候选授权，仍须以完整新摘要重新批准。
