# 阿里云短信验证码阶段 5 验收报告

## 1. 当前结论

状态：**阶段 5A 关闭态测试服部署通过，阶段 5 整体尚未验收。**

已完成：主线/工作区审计、测试服只读开关与代理审计、短信供应商指标开发、告警规则、Nginx 静态检查、
Go 全库测试与 vet、两端契约/类型/Lint/生产构建、测试/生产两种回滚 Dry Run、promtool 规则检查、
两端真实浏览器响应式与控制台错误检查，并已把 Nginx、准备度、promtool 和双环境回滚检查加入 CI。
2026-08-05 已在当前 HEAD 再次执行 Go 全库测试/vet 和两端 type-check、Lint、生产构建，全部通过；前端仅有既有
模块类型、动态导入和大 chunk 警告，没有错误。
在提交 `6527919` 后又执行一次同等范围回归：Go 全库测试/vet、两端 type-check/Lint/build、35 项阶段 5 契约和
actionlint 全部通过；阶段 5 契约仅有 Windows 环境按设计跳过的 1 项 Linux Bash 行为用例，测试服 Linux 证据已另行补齐。

新增完成：阶段 5 分支提交并同步主线、部署前备份、测试服固定代理 `/28`、可信代理配置、新后端关闭态部署、
4 条短信告警加载，以及双代理 `503/50300`、数据库/指标/日志零发送增量核验。
部署后只读验收脚本已增强为严格失败关闭，并完成正确哈希正例、错误哈希反例和 30 秒零发送观察。
2026-08-05 复跑同一 30 秒关闭态观察仍通过：API 进程稳定，短信保持关闭，发送摘要保持 `13:13:0`，Provider
调用总数保持 0，固定代理、Prometheus 和数据库摘要均无漂移；本次没有业务配置修改、服务重启或短信发送操作。
测试服管理代理上的 5 个只读短信管理 GET 接口也已完成实服核验：授权管理员请求全部成功，模板与绑定均为
`5:5:5`、发送日志为 `13:13:0`，响应未发现敏感字段或完整手机号。无权限账号返回 `403/40003`，权限满足但
双重认证失效的账号返回 `403/40031`；两组请求前后短信日志、验证码与审计总数保持 `13:1545:251`，Provider
调用指标保持 0。短期 JWT 仅在内存中生成和使用，没有输出或持久化，也没有调用 4 个短信管理写接口。
随后使用真实 Chrome 对测试服管理页面完成认证后只读复验：概览 `5:5:5:5/5:0`、模板 5、场景 5、
发送日志 13；`1440/1024/768/390` 四档均无横向溢出。页面期间短信管理请求只有 5 次 GET，写请求与非 200
响应均为 0，功能性控制台错误和页面异常均为 0，完整手机号与 DOM 内 Token 均为 0。另观察到 1 条未能关联
具体响应路径的静态资源 404 控制台提示，不影响页面和 API，本阶段作为低优先级环境观察保留。
测试服回滚材料与通知链只读预检也已固化并执行：冻结备份点 11 个文件、外部固定的 `SHA256SUMS` 摘要、
属主/权限、新旧 API 哈希、当前运行 PID/监听和 x86-64 架构均通过，三份容器快照结构有效且引用镜像仍存在，
当前 health/ready 为 `200/200`。进一步检查发现旧环境语法、基础键和短信关闭态有效，但缺少当前固定代理信任，
且仍含两项已废弃的 `SMS_TEMPLATE_CODE_*` 键，因此禁止整份恢复，静态前置状态为
`backup_env_missing_fixed_proxy_trust`。这些证据只证明恢复材料完整及当前版本健康，不证明旧版本能够成功启动。另一方面，
Prometheus 虽加载 4 条短信规则，但实际配置中的 Alertmanager 引用、容器、进程和 9093 监听均为 0；通知链状态
明确为 `receiver_configuration_required`，不能把规则加载冒充通知可达。
安全候选生成器已通过本地行为契约和默认无连接 SelfTest。项目负责人随后使用精确批准口令授权真实生成，测试服已排他创建
`candidate-20260805T015043Z.env`；候选文件 SHA-256 为 `8435f846ff2e5815bec889ac4e4c32d432acb06bb05c0e1e9c3bd6b02bb65494`，
目录/文件属主权限为 `pc:700`/`pc:600`。独立只读解析确认短信关闭、测试模式开启、固定代理精确保留、废弃模板键和重复键均为 0，
未输出敏感值。该核验现已固化为按 UTC ChangeId 执行的只读 CLI，并在测试服 Linux 通过有效候选及缺失、符号链接、错误权限、
短信开启、代理漂移、废弃键、重复键八类行为自测；真实候选经新 CLI 复验得到同一 SHA-256。生成过程未替换当前环境、未重启服务、
未发送短信；候选安全性通过不等于旧二进制运行时回滚通过。
测试服日志留存只读审计也已固化并执行：journald 正常运行、持久目录存在、`Storage` 允许持久化；文件系统约
982.24GB、可用约 354.74GB，journal 目录约 2.97GB。容量上限、磁盘保留空间、最长留存时间和单文件轮转周期四项
显式配置均缺失，因此配置完整性、运行时重载和策略验收
全部为 false。该预检固定保持独立授权门禁，不能根据配置存在自动推定获批或运行时生效；SSH 登录可能增加访问审计日志，
业务配置修改和 Provider 调用均为 0；本轮操作范围明确禁止真实短信，服务端证据只证明发送日志与 Provider 指标零增量，
不能据此证明用户侧收件事实。完整分支 Standards/Spec 双轴审查发现的部署回滚、关闭态验收、白名单可重复核验、
部署探针 Redis 副作用和证据口径问题均已修复，复核未发现新的 P1/P2。原 P3 重复 SSH 指纹校验也已收敛为共享函数，
六个远端包装器通过同一固定目标与 ED25519 指纹契约；该结论属于本地代码审查和契约证据，不替代 PR CI、独立 QA 和产品经理确认。

未完成：测试服真实短信、生产只读审计、生产部署、生产 Canary、生产开关、
24 小时观察、独立 QA、产品经理批准、PR 审查、PR CI 和合并。

2026-08-05 已按项目负责人既有授权将 `codex/aliyun-sms-phase5-canary-release` 推送至 `origin`；首次远端同步锚点为
`9a3e50e326312a166246889b5a495fd322ccc04f`，后续状态文档提交沿同一已批准 feature 分支同步。本次没有创建 PR、
合并 `main` 或改变测试服运行态。仓库 CI 仅由面向 `main` 的 `pull_request` 触发，因此分支推送没有产生可冒充为
PR CI 的运行记录。

## 2. 当前门禁

本节是阶段 5 当前状态的唯一权威摘要。其他阶段 5 文档分别保存部署、观察、代理、安全和回滚证据，不另行定义
冲突的总状态；状态变化必须先更新本表，再同步 README 的简要进度。

| 门禁 | 状态 |
|---|---|
| 阶段 4 已进入 main | 通过 |
| 本地短信指标专项测试 | 通过 |
| 仓库 Nginx 静态校验 | 通过 |
| 回滚 Dry Run | 通过 |
| 本地真实浏览器验收 | 通过：管理端短信页和用户端注册页四档无横向溢出，登录/找回密码手机端无横向溢出 |
| 测试服开关安全状态 | 通过：false/true |
| 测试服数据库与五绑定 | schema `64:0`；5 个审核/启用模板、5 个启用绑定、5 个独立模板 |
| 测试服端到端可信代理 | 关闭态通过：专网 `/28`、固定 `.2/.3`、可信配置、伪造转发头覆盖均已核验 |
| Prometheus 官方规则检查 | 通过：同版本 promtool 校验；运行实例加载 4 条短信告警，目标 up，40 个调用序列可查询 |
| 告警通知链 | 未就绪：Alertmanager 配置引用、容器、进程和 9093 监听均为 0；`sms-phase5-alertmanager-change-runbook.md` 已固化渠道决策、离线校验、关闭态部署和合成演练四道独立门禁 |
| 阶段 5 分支同步 | 通过：远端 feature 分支至少包含已复核锚点 `9a3e50e326312a166246889b5a495fd322ccc04f`，状态文档继续沿同一分支同步；未创建 PR、未合并 main |
| 阶段 5 CI 安全门禁 | CI 已加入全部 Actions 工作流的 actionlint；本地复验通过。工作流仅监听面向 main 的 PR，feature 分支推送未触发 CI，实际 CI 仍待 PR 后执行 |
| 测试服关闭态部署 | 通过：新二进制 hash 已核验，health/ready 通过，双代理发码均 `503/50300` |
| 部署后可重复核验 | 通过：精确网络/IP、数据库、指标、告警、抓取目标和观察增量均纳入脚本断言 |
| 测试服回滚材料 | 材料完整；旧环境禁止整份恢复。远端关闭态候选已排他生成并通过权限、摘要、固定代理、废弃键和重复键只读核验；当前环境未替换、服务未重启。旧二进制恢复运行时和实际回滚仍未通过 |
| 测试服日志留存策略 | 未通过：`8G/50G/14day/1day` 已于 2026-08-05 获批；最新重试再次完成主机身份核验，但仍在任何写入和重启前因 `sudo_unavailable` 失败，四项显式策略继续缺失。仓库外离线运维交接包已生成并通过测试服 Linux 回滚行为自测，自测业务配置写入、服务重启和短信发送均为 0；尚未由有权限运维执行真实变更，失败后 journald 与短信关闭态复验正常 |
| 测试服短信管理只读 API | 通过：5 个 GET 经管理代理实服返回正确；未登录 `401/40001`、无权限 `403/40003`、MFA 失效 `403/40031`；响应脱敏且数据库/审计/Provider 零增量 |
| 管理后台认证后真实数据显示 | 通过：真实 Chrome 展示 5 个模板、5 个场景、13 条日志；四档无横向溢出，短信写请求 0；保留 1 条无路径静态 404 环境观察 |
| 四个管理写 API 与五个真实场景 | 待真实 Canary 授权；本轮未调用写接口或真实供应商 |
| 真实短信 | 未授权、未执行；服务端仅确认发送日志与 Provider 调用零增量，不声称证明手机收件数 |
| 生产发布与观察 | 未开始 |
| P0/P1 | 当前本地开发未发现；最终以 QA 结论为准 |

本地验证明细：阶段 5 安全契约 16/16；管理端短信 9/9、管理员 MFA 8/8、邮件 11/11、Vite 隔离 1/1、出站 URL 4/4；
用户端短信 9/9、邮件 15/15、Vite 隔离 1/1；两端 type-check、ESLint、build 通过。前端依赖审计仍有
1 个 moderate、6 个 high 的既有风险，本阶段未执行破坏性依赖升级。

浏览器验证使用 Chrome/Playwright，覆盖 `1440×900`、`1024×768`、`768×900`、`390×844`。管理端以
本地脱敏模拟响应检查短信管理页，未使用真实 Token、手机号或验证码；用户端只打开注册、登录、找回密码页面，
未点击发送验证码。所有检查均未发现横向溢出，最终控制台错误为 0。浏览器验证结束后仅关闭本轮启动的
5173/5176 服务，既有 5174 进程保持不变。

测试服认证后浏览器复验使用现有合格管理员身份生成 10 分钟内存 Token，只在隔离浏览器上下文的
`access_token` 存储中短暂存在；Token 不进入命令参数、输出、截图、DOM 或文件，浏览器上下文结束后即销毁。
该次只浏览概览、模板、场景和日志页签，没有点击同步、状态开关、保存配置或测试发送。

本机 Windows Go 未启用 CGO，直接执行 `go test -race` 会被 Go 工具链拒绝；仓库 Linux CI 已配置 MySQL 8、
Redis 7 和 `go test -v -race -count=1 ./...`，并新增阶段 5 发布安全门禁。该竞态与隔离依赖证据必须等 PR CI
实际成功后才能记为通过，当前不以普通 `go test` 替代。

## 3. Goal 状态

Goal 必须保持 active。只有真实 Canary、生产灰度、生产开关与观察、QA/产品/审查、PR/CI/合并和 `origin/main`
核验全部关闭后，才允许标记 complete。
