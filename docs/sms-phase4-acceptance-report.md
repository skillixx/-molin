# 阿里云短信验证码阶段 4 验收报告

## 1. 当前状态

状态：**PR #320 已创建，功能提交 `ac33744` 的隔离 CI 5/5 全绿；CI 后独立规格、QA 与产品复核完成。Goal 保持 active，等待最新文档提交复验及用户合并批准。**

阶段 4 没有获得真实短信、部署或 migration 授权，未执行这些操作。

## 2. 验收门禁

| 门禁 | 状态 |
|---|---|
| 五场景矩阵 | 已完成 |
| 必要缺陷修复 | 已完成并由隔离 CI 验证 |
| Go 全量测试 | 本地通过 |
| 前端契约、类型、Lint、构建 | 通过 |
| 本地 Mock 浏览器验收 | 通过 |
| 隔离 MySQL/Redis | GitHub Actions run `30891459024` 通过 |
| Linux race | GitHub Actions run `30891459024` 通过 |
| 规格轴/标准轴代码审查 | CI 后复审通过，P0/P1=0；文档状态缺陷已修复 |
| 测试工程师验收 | 代码、自动化和隔离 CI 通过；最新文档提交 CI 全绿后正式签字 |
| 产品经理验收 | 代码与 CI 通过；P2/P3 已书面接受，最新文档提交 CI 全绿后正式批准 |
| PR/CI | PR #320 已创建；功能提交 `ac33744` 的 5 个 CI Job 全部通过 |
| 合并 | 未授权，未执行 |
| 真实短信 | 未授权，0 条 |

## 3. 缺陷统计

| 级别 | 已发现 | 已修复 | 当前未关闭 |
|---|---:|---:|---:|
| P0 | 0 | 0 | 0 |
| P1 | 8 | 8 | 0 |
| P2 | 9 | 7 | 2（产品经理接受拆分为后续任务） |
| P3 | 2 | 1 | 1（产品经理接受后续增强） |

- P1：缺少手机号+场景冷却；并发错误次数可越界；管理员手机 200 畸形响应可误报成功；负向 HTTP 证据不足；
  新增 E2E 失败日志可能泄露敏感值。均已按首轮审查意见修复并补测试。
- 规格复审新增 P1：手机号登录账号完整落入 `user_login_logs`；现已改为写入前脱敏并增加单元/CI 断言。
- 安全复审补充 P1：短手机号、短邮箱或异常账号可能绕过展示层掩码；现已使用登录日志专用失败关闭脱敏器并补否定用例。
- QA 收尾复核补充 P1：公开手机发码/密码重置曾使用可伪造 XFF 作为 IP 桶；现已统一接入可信来源解析器，
  非可信直连只认 `RemoteAddr`，并补轮换伪造头仍在第十一次返回 429 的回归用例。
- P2：管理后台本地代理硬编码、文档状态冲突、错误文案/作用域冲突、Redis 错误语义不清、用户端代理缺测试均已修复。
- `P2-SPEC-DOC-01`：阶段 4 测试矩阵误用旧表名 `sessions`，现已统一为权威表 `user_sessions`。
- `QA-4-DOC-01`：PR/CI 完成后验收材料状态未及时回填；现已更新 PR #320、功能提交 `ac33744`、
  GitHub Actions run `30891459024` 及 5/5 成功证据。最终关闭证据以本次文档提交的最新 HEAD CI 全绿为准。
- 产品经理已接受拆分的 P2：auth 权威兼容契约仍要求手机验证码使用 `verification_codes.target_value`；改为 HMAC 需要
  新 migration 和停发窗口，本阶段没有 migration 授权。短信日志、Redis、响应和审计已保持脱敏。
- 产品经理已接受拆分的 P2：新代码不再写入完整登录账号，但历史 `user_login_logs` 不会在无维护窗授权时自动回填；
  建议作为独立数据治理任务处理，阶段 4 只验收新写入路径。

产品经理书面决定：以上两个 P2 均不在本阶段无 migration/维护窗授权下暗改。前者另立手机目标 HMAC 化、
双读切换、停发与回滚任务；后者另立备份、幂等脱敏回填、前后计数和审计任务。P3 管理员 MFA 审计来源 IP
另立 Handler 可信 IP 透传增强。本阶段 PR 必须在风险说明中保留这些决定。

CI 后独立复核确认：P0=0、P1=0；两个 P2 和一个 P3 已由产品经理书面接受为后续独立任务，不阻断阶段 4 合并。

## 4. 当前结论

当前可以确认：五场景代码、Mock、隔离 MySQL 8/Redis 7 HTTP E2E、Linux race 和页面行为已经形成可执行验收闭环；
GitHub Actions run `30891459024` 在功能提交 `ac33744` 上 5/5 通过。CI 期间发现的邮箱夹具约束、未注册邮箱
`404/40404` 契约和 `user_sessions` 表名断言均已修复并经后续 CI 验证。
当前不能确认：部署态阿里云受理、真实收件和 PR 合并闭环。
部署态还必须单独核对实际反向代理地址已进入 `TRUSTED_PROXY_IPS`，且代理覆盖单值 `X-Real-IP` 并删除
XFF/Forwarded；本阶段没有部署授权，当前只确认代码与本地契约。

> 阶段 4 本地与隔离环境验收通过，真实短信部署态验证待单独授权。

## 5. 回滚

本阶段无 migration。回滚应用提交并保持 `SMS_ENABLED=false` 即可。不得回滚或清理原工作区用户改动；
不得通过关闭安全门禁来恢复短信发送。
