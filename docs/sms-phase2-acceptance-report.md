# 阿里云短信验证码阶段 2 测试与验收报告

## 当前结论

阶段 2 的后端实现、本地自动化和预合并修复远程 CI 已通过。PR #315 预合并修复提交 `9686433` 对应的 GitHub Actions 运行 #373 已成功完成，后端任务在 MySQL 8、Redis 7 环境执行 `go test -v -race -count=1 ./...`，管理后台和用户控制台构建也均成功。隔离测试服已部署提交 `e637da7` 并完成 migration `000059`、模板幂等同步和五场景绑定。真实发送前置检查确认管理员 259 的原绑定手机号不是真实可收件号码，因此窗口安全停止且未调用 SendSms。随后发现并在当前工作树修复“换绑联系方式仍继承旧管理员 MFA”的 P1，尚待推送、PR 最新 CI 和重新部署。真实阿里云短信受理、手机实际收件、独立测试工程师签署、产品经理确认、GitHub 独立正式评审及合并仍未完成。因此当前只能确认“阶段 2 代码开发及部署配置验证已形成”，不能报告完整验收完成，也不得进入阶段 3。

## 最终报告

| 项目 | 当前证据 |
|---|---|
| 阶段 | 阿里云短信验证码阶段 2 |
| 基线 commit | `origin/main@3aa8f3e` |
| 工作分支/worktree | `codex/aliyun-sms-phase2-management-api` / `D:\molingproject\molin-sms-phase2` |
| 完成任务 | 模板只读同步、五场景管理、九个 API、四权限、安全测试发送、审计、文档与自动化资产 |
| 新增或变更接口 | `/api/admin/sms` 下九个阶段 2 接口；五个现有手机入口路径不变 |
| 新增权限 | `sms:template:view/manage/sync/test` |
| 新增 migration | `000059_add_sms_phase2_management` |
| 本地自动化验证 | 16 路幂等并发、场景 CAS、同步去重、五场景独立数据库选模、九接口权限/MFA、限流失败关闭等专项测试，以及全库 `go test`、`go vet`、`go mod verify`、敏感扫描和补丁格式检查通过 |
| 数据库验证 | 静态契约通过；本机因无 MySQL/Docker 安全 SKIP；PR #315 的 Linux CI 在 MySQL 8 服务中通过 `SMS_MIGRATION_TEST_DSN` 启用并完成 `1→59→58→59` 矩阵 |
| 安全与权限验证 | 九接口未登录/缺权限/MFA 绕过矩阵、白名单、双维限流、Redis 错误失败关闭、幂等冲突、并发单外呼、审计脱敏和响应内部摘要隐藏已通过本地测试；PR #315 的 Linux CI 在 Redis 7 服务中通过 `SMS_REDIS_INTEGRATION_TEST=1` 启用真实 Lua 限流门禁并通过 |
| 真实阿里云验证 | 已获受控窗口授权，但因管理员绑定号码不是真实可收件号码而在 SendSms 前安全停止 |
| 手机实际收件证据 | 未执行，不能由模板同步、Mock 或 Code=OK 替代 |
| 未验证事项 | 真实阿里云受理、五场景实际收件、隔离测试环境运行时敏感扫描、测试工程师签署、产品经理确认、正式评审及合并 |
| P0/P1 缺陷 | 新发现的“换绑联系方式继承旧管理员 MFA”P1 已在当前工作树修复并通过本地回归；尚待 PR 最新 CI、重新部署和独立 QA 复证 |
| 残余风险 | 真实供应商与运营商链路尚未验证；PR 尚未完成独立验收、正式评审和合并 |
| 测试工程师结论 | 待验收 |
| 产品经理结论 | 待确认 |
| 当前 SMS_ENABLED 状态 | `false`；未改动环境，未产生短信费用 |
| 是否满足阶段 2 完成标准 | 否 |
| 是否允许进入阶段 3 | 否 |
| Git 提交 | 阶段 2 提交记录以 PR #315 的提交列表为准；所有提交说明均使用中文 |
| PR 状态 | [PR #315](https://github.com/skillixx/-molin/pull/315) 已创建且无冲突；预合并修复提交三项 CI 全部成功，独立正式评审及合并仍未完成 |

## 证据等级

- 单元/Mock：证明本地规则、状态机和供应商适配边界。
- 本地接口：当前覆盖路由、中间件及 handler，不代表部署环境。
- MySQL/Redis：本机缺少真实执行；[GitHub Actions 运行 #373](https://github.com/skillixx/-molin/actions/runs/30796827755) 已在 MySQL 8、Redis 7 服务中启用两项显式测试门禁，并以 Linux `-race` 全库测试成功结束。
- 阿里云模板查询与同步：已执行且重复同步幂等；不等于短信受理。
- 阿里云 SendSms 受理：未执行。
- 手机实际收件：未执行。

真实验证必须在隔离测试环境、500 条预算上限、白名单和单次授权窗口内执行，完成后立即恢复 `SMS_ENABLED=false`。

## 预合并工程审查

Codex 已按 `origin/main...codex/aliyun-sms-phase2-management-api` 完成代码、迁移、接口契约、测试和文档的预合并审查。审查中发现并修复：历史 `submitted_at` 回填错误、模板列表 N+1 查询、供应商忽略上下文时同步超出硬截止时间、固定签名为空仍可启用模板及其错误码映射。真实验证准备期间又发现并修复换绑手机号/邮箱后保留旧管理员 MFA 的 P1；本地认证模块、全仓测试、vet、依赖校验和敏感扫描通过，待 PR 最新 CI 与重新部署复证。

该结论属于开发侧工程审查，不替代独立测试工程师验收、产品经理业务确认或 GitHub 审阅批准。
