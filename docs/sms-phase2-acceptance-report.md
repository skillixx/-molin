# 阿里云短信验证码阶段 2 测试与验收报告

## 当前结论

阶段 2 的代码、迁移、接口契约、本地自动化、远程 CI、隔离测试部署和受控真实短信验证已经形成证据。当前 PR #315 远端提交为 `79ac4d0`，对应 GitHub 三项检查全部通过且无合并冲突；本地全仓测试、vet 和依赖校验也已通过。隔离测试服已部署该提交对应的 Linux 二进制，远端 SHA-256 为 `2e869f7ad9a4f93ac1cb252ed1a03de4fc20d05954c4bea1c5af0b6bca1c6b14`，数据库部署前后均为 `59:0`，本次未执行 migration。

2026-08-04 只读同步得到 5 个审核通过的独立验证码模板，重复同步为新增 0、更新 0、未变化 5。管理 API 已将 `register/login/reset_password/bind_phone/admin_verify` 分别绑定模板编码后四位 `5137/0141/0117/0118/5093`，5 个有效场景对应 5 个不同的模板 ID。

在已授权的隔离窗口内完成一次管理测试发送和五业务入口发送，本轮共新增 6 条阿里云 `Code=OK/accepted` 记录，失败 0。用户确认实际收到 6 条短信，并确认统一签名“爱斯琴山东网络科技”及六条场景文案均正确。窗口结束后已恢复 `SMS_ENABLED=false`、`SMS_TEST_MODE=true` 和原白名单，健康检查 200，运行时敏感信息检查通过。

2026-08-04 已在 `SMS_ENABLED=false` 下完成九个管理 API 的部署态 HTTP 复核：读取接口均为 HTTP 200/业务码 0；重复同步为新增 0、更新 0、未变化 5；场景和模板状态使用过期版本分别返回 HTTP 409 且数据库无变更；测试发送返回 HTTP 503，发送日志前后均为 13 条。历史“五场景共用单模板”P1 已关闭，独立 QA 最终签署为 P0=0、P1=0、P2=0、P3=0，产品经理批准阶段 2 产品验收，正式代码评审通过。六份最终验收文档提交 `dd5df2d` 后，PR #315 已于 2026-08-04 Squash 合并至 `main@9e50ee1`，阶段 2 正式闭环。`register/login/reset_password` 的真实验证码消费 E2E 按权威规划属于阶段 4 待办。

## 最终报告

| 项目 | 当前证据 |
|---|---|
| 阶段 | 阿里云短信验证码阶段 2 |
| 基线 commit | `origin/main@3aa8f3e` |
| 工作分支/worktree | `codex/aliyun-sms-phase2-management-api` / `D:\molingproject\molin-sms-phase2` |
| 当前提交 | 远端及测试服代码基线 `79ac4d0`；本报告更新在其后本地工作树 |
| 完成任务 | 模板只读同步、五场景管理、九管理 API、四权限、安全测试发送、审计、迁移、文档与自动化资产 |
| 新增或变更接口 | `/api/admin/sms` 下九个阶段 2 接口；五个现有手机入口路径不变 |
| 新增权限 | `sms:template:view/manage/sync/test` |
| 新增 migration | `000059_add_sms_phase2_management` |
| 自动化验证 | 全仓 `go test`、`go vet`、`go mod verify`、并发/幂等/权限/MFA/限流/脱敏专项和敏感扫描通过 |
| 数据库验证 | `79ac4d0` 的三项 GitHub 检查全部通过；测试服部署前后 schema 均为 `59:0`，本次未执行 migration |
| 安全与权限验证 | 九接口 401/403/MFA 矩阵、最小权限、白名单、Redis 双维限流、失败关闭、审计脱敏、并发单外呼和幂等冲突通过 |
| 真实阿里云验证 | 本轮 `accepted=6`、`failed=0`、`provider_code=OK=6`；一次管理测试发送及五业务入口均已受理 |
| 手机实际收件证据 | 用户确认收到 6 条，并确认统一签名和六条场景文案均正确；未记录验证码及完整手机号 |
| 运行时敏感扫描 | 通过；短信发送日志、短信审计、验证码哈希和 API 日志的完整手机号/明文验证码/AccessKey/Token 计数均为 0 |
| 未验证事项 | 注册/登录/找回密码验证码消费 E2E 转入阶段 4 待办；阶段 2 当前只剩验收文档提交、推送及 PR 合并流程 |
| 缺陷分级 | 独立 QA 最终签署：P0=0、P1=0、P2=0、P3=0 |
| 残余风险 | 测试服仍含废弃短信键名，未来启用前需另行清理；仅完成白名单小流量验证，不代表长期送达率 |
| 测试工程师结论 | **阶段 2 QA 通过，限定代码基线 `79ac4d0`**；P0=0、P1=0、P2=0、P3=0，消费 E2E 转入阶段 4 |
| 产品经理结论 | **阶段 2 产品验收批准**；五独立模板、五场景、统一签名、六条文案及九 API 通过，PR 合并后允许进入阶段 3 |
| 当前 SMS_ENABLED 状态 | `false`；`SMS_TEST_MODE=true`；原白名单 1 个号码已恢复 |
| 签名数据库字节核验 | 五条场景绑定的 `sign_name` UTF-8 十六进制均为 `E788B1E696AFE790B4E5B1B1E4B89CE7BD91E7BB9CE7A791E68A80`，对应统一签名“爱斯琴山东网络科技” |
| 费用口径 | 本轮 6 次受理可能计费，低于本轮 10 条上限和总预算 500 条；实际账单金额未核对 |
| 是否满足阶段 2 完成标准 | **是**；功能、QA、产品、正式评审、验收文档、CI 和合并门禁全部满足 |
| 是否允许进入阶段 3 | **是**；阶段 3 已基于 `main@9e50ee1` 启动独立前端分支 |
| PR 状态 | [PR #315](https://github.com/skillixx/-molin/pull/315) 已于 2026-08-04 Squash 合并，`main` 提交 `9e50ee1` |

## 证据分级

- 单元/Mock：证明内部规则、状态机、并发和供应商适配边界。
- MySQL 8/Redis 7/Linux race：`79ac4d0` 对应的 GitHub 三项检查全部通过。
- 模板查询与同步：真实阿里云只读调用已完成且重复同步幂等。
- SendSms 受理：本轮 6 条记录为 `Code=OK/accepted`，只证明阿里云受理。
- 手机实际收件：用户明确确认收到 6 条且签名、六条场景文案正确，不由 `accepted` 推断。
- 阶段验收：必须由独立测试工程师、产品经理和正式代码评审分别形成结论，不能由开发侧自签。

## 预合并工程审查

开发侧审查已修复历史 `submitted_at` 回填、模板列表 N+1、同步硬截止、空签名启用、错误码映射、换绑联系方式继承旧管理员 MFA，以及短信管理写操作缺少前置审计等问题。两轴预合并评审继续发现额外变量模板可能放行、结果审计失败会把已生效操作误报为 500；`d440e29` 已增加同步/启用/绑定/发送四层变量精确校验，并把审计语义收敛为“请求审计失败关闭、结果审计失败安全告警且保持真实业务结果”。需求复审确认两项代码缺陷完整关闭，规范复审未发现新的阻断代码问题；`79ac4d0` 的本地验证、GitHub 三项检查、隔离部署和九 API HTTP 均已通过。独立 QA、产品经理和正式代码评审已经分别形成通过结论；15 条已推送历史提交仍缺“模块：描述”前缀，不得自行 force push，后续须按项目规则提交验收文档并完成 PR 合并。
