# AI 网关 G8 测试服务器只读基线报告（2026-08-12）

> 本报告只记录测试服务器低敏聚合事实。当次核验未观察到文件、环境变量、数据库、缓存、队列或服务变更信号，但旧采集器曾执行环境文件，不能据此给出绝对无副作用证明；未连接生产、调用付费上游、发送真实通知或开放客户流量。

## 1. 执行边界

| 项目 | 结果 |
|---|---|
| 最终有效 ChangeId | `CHG-G8-TEST-READONLY-20260812-002` |
| 目标 | `pc@8.130.9.163:10003` |
| SSH | 固定 known_hosts、`BatchMode=yes`、单次连接、零重试 |
| 采集器版本 | 当次使用未提交的本地候选脚本，源码摘要没有保留；该候选曾读取并执行环境文件。仓库最终版 `infra/scripts/audit-ai-gateway-g8-test-server-readonly.sh` 已移除执行行为。因此只能确认未观察到远端写入，不能把当次执行追认为具备绝对无副作用证明 |
| 业务请求 / 上游费用 | `0 / 0 CNY` |
| 观察到的远端写入或服务变更信号 | `0`；因当次采集器曾执行环境文件，不能据此给出绝对无副作用证明 |
| 首个候选 | `CHG-G8-TEST-READONLY-20260812-001` 因本地 PowerShell 与远端引号兼容问题部分执行后失败关闭，未重试；未观察到写入或服务变更信号 |

## 2. 主机、制品与运行状态

| 项目 | 只读结果 | 判定 |
|---|---|---|
| 远端用户 / 主机 | `pc / pc-Z790-UD-AX` | 与用户批准的连接端点一致 |
| machine-id 摘要 | SHA-256 `b60555f0d8d48731b657d21b2e54559d263210688125ae56a4d662fc4d7278d4` | 已记录低敏身份摘要 |
| SSH ED25519 指纹 | `SHA256:q5xYBX+tB+VPPCSTYFN6GTIbdn4sPicQslLLbkxRG+I` | 连接与本机既有固定 known_hosts 匹配；此前没有独立 CMDB/运维批准值可供二次比对，当前仅作为后续基线 |
| 部署目录 | `/home/pc/molin` 存在 | PASS |
| Git HEAD | 目录不是可识别 Git 工作树 | UNKNOWN |
| API 进程 / 监听 | `0 / 0` | FAIL |
| health / ready | 均不可达 | FAIL |
| API 二进制 | SHA-256 `97c4d948cd46ca6816ba59feb5466db2e9c09d8bfda136097aabccc2516a6c72`；权限 `700` | 摘要与 G7 测试服制品一致，不证明 G8 已部署 |
| 环境文件 | 普通文件，`pc:pc`，权限 `600`，共 76 个变量键 | 文件权限 PASS；采集器读取变量名，并对白名单凭据值仅在内存中做摘要比较，不输出原值或摘要 |

## 3. 基础设施、监控与账务

当前 `pc` 无直接 Docker 权限，`sudo -n docker` 也不可用。因此以下项目没有权威运行证据，禁止把 `UNKNOWN` 当作 0 或 PASS：

| 项目 | 结果 |
|---|---|
| MySQL 版本、schema、核心表规模 | UNKNOWN |
| Redis / RabbitMQ 健康与积压 | UNKNOWN |
| Bifrost 两节点与负载均衡 | UNKNOWN |
| Prometheus targets / 22 条规则 | UNKNOWN |
| Grafana 16 面板 | UNKNOWN |
| Alertmanager 路由 | UNKNOWN |
| G7/G8 三项账务差额 | UNKNOWN |
| 七类账务异常、hold、Outbox、补偿 | UNKNOWN |

远端未部署 `ai-gateway-reconcile` 可执行文件，不能在本轮只读范围内用源码构建或上传替代。

## 4. 备份与凭据

| 项目 | 结果 | 后续动作 |
|---|---|---|
| 最新可发现备份 | 时间 `2026-08-04 20:00:59+08:00`，大小 `15575` 字节，SHA-256 `f4c03966aaae727e3accf8fe6214445c64ececdbf57606ec74fe87edbccf51ba`，可读 | 仅证明文件可读取，未执行恢复；进入部署前需要新备份和隔离恢复验证 |
| MySQL 密码 | 与历史 README 测试密码摘要不相同 | 保留轮换审计记录 |
| RabbitMQ 密码 | 与历史 README 测试密码摘要不相同 | 保留轮换审计记录 |
| MinIO Secret | 与历史 README 测试密码摘要不相同 | 保留轮换审计记录 |
| Redis | 当前环境未配置密码 | 测试服也应设置独立强凭据并验证客户端切换 |
| SSH | 密钥登录有效；账号仍有密码，sshd 是否允许密码认证 UNKNOWN | 核对 sshd 生效配置；如无必要则禁用密码认证或轮换密码 |

当次采集器读取白名单凭据值并仅在内存中计算摘要比较；输出只包含分类，不包含凭据原值或摘要。由于当次采集器还曾执行环境文件，本报告将其作为执行版本限制明确披露；仓库中的修订版脚本已改为纯文本解析，禁止执行环境文件内容。

## 5. 缺陷与门禁

- P0：0。
- P1：3。
  1. API 当前停止且 health/ready 不可达。
  2. 部署制品仍对应 G7 摘要，无法追溯源码提交，未证明 G8 已部署。
  3. MySQL、Redis、RabbitMQ、Bifrost、监控和账务均缺少运行证据，测试服 G8 验收失败关闭。
- P2：5。
  1. 最新可发现备份距审计时间约 8 天，且未验证恢复。
  2. Redis 未配置密码。
  3. SSH 账号仍设置密码，服务端密码认证策略未知。
  4. SSH 指纹与 machine-id 已固化为本次基线，但缺少仓库外 CMDB 或运维批准值的独立二次比对。
  5. 当次未提交采集器的源码摘要没有保留，后续只能依据低敏输出和最终脚本复核结果，不能精确重建当次执行载荷。

当前结论不是“测试服 G8 通过”，更不是生产上线。`G8_ENGINEERING_READY` 仍由已合并代码、CI 和隔离演练证明；`G8_COMMERCIAL_ACCEPTED` 仍未开始。

## 6. 待批准的测试服恢复候选

后续写操作必须使用新的 ChangeId，至少固定：

- 当前 `origin/main` 源码提交与 Linux API、只读对账二进制 SHA-256；
- 测试服真实服务控制方式和 Docker/数据库只读权限；
- 变更前新备份及隔离恢复证据；
- API、Bifrost 双节点、Prometheus、Grafana、Alertmanager 的启动与回滚命令；
- `AI_GATEWAY_TRAFFIC_ENABLED=false` 和边缘总闸关闭；
- Fake 上游、零真实客户、零付费请求；
- health/ready、schema `66:0`、三项差额和全部积压为 0 的停止条件。

未取得上述精确批准前，不上传制品、不启动服务、不修改配置、不执行 Migration、不轮换凭据。
