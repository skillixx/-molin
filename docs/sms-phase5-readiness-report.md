# 阿里云短信验证码阶段 5 准备度报告

## 1. 结论

阶段 5 分支已同步 `origin/main@60b569f5048f8c3e781f51052b4b853c20c7c4f7`，合并后提交为 `b21a23c`。
2026-08-04 已按项目负责人授权完成测试服固定代理、监控规则和后端关闭态部署。当前结论为：**测试服阶段 5A
关闭态部署与核验通过；生产环境尚未审计，真实 Canary 和短信开关均未授权。**

本轮未执行 migration，未打开短信开关，未调用短信供应商，真实短信与新增费用均为 0。

只读快照已经固化为 `scripts/verify-sms-phase5-test-server-readonly.ps1`。部署后版本会精确核对二进制哈希、
专用 `/28`、两个固定代理 IP、模板环境覆盖、数据库 5/5/5、40+5+5 指标序列、敏感标签、4 条告警、
Prometheus 抓取目标以及观察窗口前后发送增量；任一条件不符即非零退出。脚本不输出环境文件、凭据、手机号或
验证码。观察结束还会复核 API PID 未变化、短信仍关闭、三处 health 和 Prometheus 目标仍健康，避免用跨重启的
两个计数点形成弱证据；外部 PR CI 不自动持有测试服 SSH 权限。

## 2. Git 与工作区基线

| 项目 | 结果 |
|---|---|
| 开发分支 | `codex/aliyun-sms-phase5-canary-release` |
| 独立工作树 | `D:\molingproject\molin-sms-phase5` |
| 开发基线 | 阶段 5 开发提交 `3853377`，合并主线后的当前提交 `b21a23c` |
| 远端主线 | 已同步 `origin/main@60b569f` 并完成全量回归；尚未推送或创建 PR |
| 原工作区保护 | `D:\molingproject\molin` 的 13 项既有未提交内容未触碰 |
| migration | 复用 `000058`、`000059`；阶段 5 当前不新增 migration |
| API 契约 | 九个短信管理 API 与五业务入口不变，无需修改权威 API 文档 |

## 3. 测试服只读快照

审计时间：2026-08-04（Asia/Shanghai）。输出只保留布尔、计数、哈希和非敏感拓扑。

| 检查项 | 结果 |
|---|---|
| API 健康 | `GET /api/health` 为 200 |
| API 就绪与版本 | `GET /api/ready` 为 200；`GET /api/version` 返回 `0.1.0` |
| API 运行方式 | 宿主机进程，工作目录 `/home/pc/molin` |
| API 二进制 SHA-256 | 部署前 `c18aa8d0efe51e2b9cccf924b275983741dcd5194fa3bb25e1d292888b926cc9`；部署后 `4ade3d34a7b9473a23cbda80c4a4451192725da66caa2dc09aab454c05fdd8b0` |
| 部署目录 Git 身份 | 不可用；当前部署证据以 API 版本和二进制 SHA-256 为准，不能把目录推定为某个 commit |
| `APP_ENV` | `test` |
| `SMS_ENABLED` | `false` |
| `SMS_TEST_MODE` | `true` |
| 白名单数量 | 1，不记录号码 |
| 阿里云凭据、签名、HMAC | 均已注入，仅验证存在性 |
| MySQL 与数据库 schema | MySQL 8 容器；schema `64:0`，高于短信 `000059` 且非 dirty；阶段 5 无需补 migration |
| Redis | 运行镜像 `redis:7`，容器内版本 `7.4.9`；未读取密码或业务键 |
| 模板摘要 | 5 个模板，审核通过 5、启用 5 |
| 场景摘要 | 5 个绑定，启用 5、启用模板去重数 5 |
| 发送日志摘要 | 13 条 accepted、0 条 failed；与阶段 2 历史 7+6 证据一致，本轮未新增 |
| 管理/用户 Nginx | 均覆盖单值 `X-Real-IP`，删除 XFF/Forwarded |
| 前端容器来源 | 专用网络 `molin-sms-proxy`：管理端 `172.20.250.2`、用户端 `172.20.250.3` |
| API 上游 | 两容器均通过专网 host gateway `172.20.250.1:8080` 访问宿主机 API |
| `TRUSTED_PROXY_IPS` | `172.20.250.0/28`，运行进程已核验生效 |
| 前端部署身份 | 管理端和用户端均为 `main-30c3e43` 镜像 |
| Prometheus | `prom/prometheus:v3.12.0-distroless` 就绪；4 条短信告警已加载，抓取目标为 up，40 个调用序列可查询 |
| 短信管理只读 API | 管理代理上的 summary、模板列表/详情、场景和发送日志 5 个 GET 全部通过；模板/绑定 `5:5:5`，发送日志 `13:13:0` |
| 管理接口鉴权负例 | 未登录 `401/40001`；无权限 `403/40003`；有权限但 MFA 失效 `403/40031` |
| 只读 API 副作用 | 请求前后短信日志/验证码/审计为 `13:1545:251`，Provider 指标为 0；账号、数据库和短信管理资源零修改 |

部署前私有备份位于 `/home/pc/molin/backups/sms-phase5-20260804T120056Z`，目录权限 700、文件权限 600，
`SHA256SUMS` 全部校验通过。备份含旧 API、旧环境文件、三容器 inspect、网络/路由、Prometheus 配置与规则、
短信表只读导出；文档不保存环境值或业务数据。

补充只读查询过程中，为计算公开版本响应哈希曾短暂创建 `/tmp/molin-phase5-version.json`，同一命令已立即删除。
该文件只包含 `/api/version` 的公开响应，没有配置、凭据或业务数据，未引起服务重启或状态变化；后续查询改为流式读取。
测试服 systemd journal 当前占用约 2.3G。新增固定测试服只读审计确认 journald 正常运行、持久目录存在且磁盘用量可查询，但合并配置中的 `SystemMaxUse`、`SystemKeepFree`、`MaxRetentionSec`、`MaxFileSec` 均未显式配置，因此固定输出 `log_retention_policy_verified=false`；日志策略变更仍需独立授权。

回滚与通知专项只读预检确认：备份目录保持 `pc:700`，11 个固定文件均为安全权限、无符号链接，清单自身摘要
与仓库固定锚点一致且文件校验通过；旧 API、当前磁盘文件及运行 PID/监听与 x86-64 架构匹配，三份容器快照
结构有效且引用镜像仍存在。旧环境语法、基础键和短信关闭态通过，但缺少固定代理信任并含两项废弃模板键，固定输出
`rollback_environment_wholesale_restore_allowed=false`、`rollback_static_blocker=backup_env_missing_fixed_proxy_trust`。
因此材料完整不等于静态前置通过；旧二进制启动仍未验证。测试服没有 Alertmanager 配置引用、容器、进程或 9093
监听，通知链状态为 `receiver_configuration_required`；当前只能证明规则计算存在，不能证明通知可达。
回滚候选生成器已通过离线 SelfTest 和行为契约：临时夹具证明会强制关闭短信、保留固定代理、删除废弃模板键、排他
创建并拒绝不可信代理配置。真实远端生成未获授权，测试服没有新增候选文件。

## 4. 剩余门禁

测试服代理阻塞已经关闭。仍未完成的内容为：Alertmanager 接收渠道配置与通知演练、实际应用回滚演练、真实短信
Canary、生产只读基线与发布、24 小时观察、独立 QA、产品经理确认、PR/CI/代码审查和合并。
`SMS_ENABLED=true`、真实短信、通知接收渠道/Secret、服务回滚、生产变更及 Git 推送仍需分别授权。

## 5. 准备度门禁

| 门禁 | 状态 |
|---|---|
| 阶段 4 已合并主线 | 通过 |
| 本地专项测试 | 通过 |
| 本地浏览器响应式检查 | 通过：管理端短信页、用户端注册/登录/找回密码；最小 390px，无横向溢出、控制台错误 0 |
| Nginx 仓库静态契约 | 通过 |
| 短信 Prometheus 指标与规则 | 通过：40 个调用序列、5 个 sum、5 个 count；4 条短信告警加载，抓取目标 up |
| 告警通知链 | 未就绪：Alertmanager 引用/容器/进程/9093 监听均为 0；渠道决策、离线配置、关闭态部署和合成演练已在 `sms-phase5-alertmanager-change-runbook.md` 拆成四道独立门禁 |
| 测试服实际 Nginx 头 | 通过 |
| 测试服可信代理配置 | 通过：专网 `/28` 生效，直连伪造头忽略，代理伪造 XFF 被覆盖 |
| 测试服日志留存策略 | 未通过：journald 运行与持久化正常，但容量、保留空间、留存时间和轮转周期四项显式策略均缺失；只读审计和独立变更门禁已固化 |
| 测试服部署 | 关闭态部署通过；双代理发码入口均为 `503/50300`，数据库/指标/日志无发送增量 |
| 可重复关闭态审计 | 通过：错误二进制哈希失败关闭；30 秒窗口发送日志保持 `13:13:0`、Provider 指标保持 0 |
| 测试服回滚材料 | 材料完整，容器快照与引用镜像存在；旧环境整份恢复被禁止。安全候选生成器离线契约通过，但远端候选未生成，静态前置=false、运行时=false、实际回滚未执行 |
| 测试服管理 GET 与页面 | 通过：5 个只读端点、未登录/无权限/MFA 三类拒绝契约及响应脱敏均已实服核验；真实 Chrome 展示 5 模板、5 场景、13 日志，四档无横向溢出，写请求 0；保留 1 条无路径静态 404 环境观察 |
| 生产只读基线 | 待单独授权和生产目标信息 |
| 生产灰度/观察 | 未开始 |
