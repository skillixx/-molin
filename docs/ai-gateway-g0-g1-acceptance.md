# 墨灵 AI 网关 G0/G1 阶段验收记录

> 日期：2026-08-03
> 分支：`feature/bifrost-ai-gateway-v2`
> G1 起始提交：`1e96584`

## 1. G0 产品签收

产品经理确认采纳以下证据：

- `tests/audit-stage1-final.md`：核心商业闭环 37/37 通过，原有 P1 已关闭。
- `tests/audit-stage1-closing-confirm.md`：管理员封禁/解禁、旧 Token 失效、越权和未登录拒绝等收口验证通过。
- `docs/frontend-acceptance-stage1-pm-review.md`：管理端和用户端第一阶段前端业务验收通过。

签收范围：测试环境中的注册/登录、实名认证、角色权限、商品与会员规则、充值功能回调、钱包、订单、购买扣费、财务流水和用户资产生成核心闭环。

产品结论：**G0 产品验收通过，可作为进入 AI 网关 G1 的前置条件。**

边界：本签收不包含真实支付机构回调、生产部署、真实 AI 上游调用和生产可用性验收。

## 2. G1 本地复审

产品经理第三轮复审结论：本地设计和代码 `P0=0、P1=0`，确认公开接口、逻辑/执行模型、正交状态、Usage、执行尝试、账本映射、Project 预算和 G1/G2 边界已经冻结；未发现进入 G2。

测试工程师第三轮复审：本地 QA 条件通过，`P0=0、P1=0`；Migration 可重入、Request ID、Native/Bifrost 清洗、逻辑模型改写、结果未知映射和脚本批准门均通过只读复核。复审后列出的四项 P2 已继续修复：GORM 复合索引元数据、漂移索引形状失败关闭、Handler 装配错误回归测试和 Migration 注释精度。第四轮发现 MySQL 不支持 prepared `SIGNAL` 后，动态语句已改为兼容的 `SELECT/ALTER` 分支，并补充同名漂移索引失败关闭验收。

当前自动化证据见 `docs/ai-gateway-g1-poc-report.md`。本地自动化或 Fake 上游通过不等于 G1 最终通过。

## 3. G1 最终硬门

以下全部关闭后，测试工程师和产品经理才能最终双签：

1. 测试 Linux 部署当前 Bifrost 配置并验证内部鉴权。
2. 百炼和 OpenRouter 的真实 JSON、SSE、Usage 与失败语义验证。
3. 单节点退出、恢复和配置/镜像回滚演练。
4. 隔离的真实 MySQL 8 Migration、约束和保留结构后 re-up 验证，禁止操作项目测试库。
5. 百炼/OpenRouter 真实端到端调用证据作为功能和稳定性观察，失败轮次永久保留。
6. 固定响应受控上游完成 5 组预热和 20 组 Native/Bifrost 配对性能硬门，JSON/SSE 分别满足 20ms/30ms。
7. 最终敏感扫描、全仓测试和 Git 暂存区复核。

受控性能硬门已于 2026-08-03 通过：预热 5 组，正式 20 组/80 次全部成功，JSON 配对差值 P95 `1.991ms`，SSE TTFT 配对差值 P95 `1.766ms`，证据 SHA256 为 `a5e209c06b8e2540119a1c443cca0ed646d9d0891041cc854aa01b10a3a00058`，独立复算一致。

最终 QA 复审结论为 `P0=0、P1=0、P2=0`，G1 QA 签收通过。最终产品经理复审结论为 `P0=0、P1=0、P2=0`，G0/G1 产品签收通过，允许仅将本阶段 37 个目标文件提交并推送 `origin/feature/bifrost-ai-gateway-v2`。

当前 G0/G1 最终结论：**通过**。该结论不授权进入 G2，不代表生产可用，不授权合并 `main`、生产部署、生产流量切换或生产密钥操作。
