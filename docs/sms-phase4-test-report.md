# 阿里云短信验证码阶段 4 测试报告

## 1. 测试快照

| 项目 | 结果 |
|---|---|
| 测试基线 | `origin/main@2c2fcbe` |
| 测试提交 | 当前阶段 4 未提交工作树；提交后需回填 commit |
| 本地环境 | Windows、Go 1.26.5、Node/npm；无本地 Docker/MySQL/Redis |
| 数据库版本 | 测试代码顺序安装到 `000059`；本阶段无 migration |
| `SMS_ENABLED` | 未修改配置，默认 `false` |
| 外部 Sender | Go 自动化使用 Mock Sender |
| 阿里云连接 | 否 |
| 真实短信 | 否，发送 0 条、费用 0 |
| 浏览器 API | Playwright 路由 Mock，仅本机 Vite |

## 2. 已执行结果

### 2.1 后端

- `go test ./... -count=1`：通过。
- 五场景 HTTP+Mock E2E 测试：已编译；本机因无隔离 MySQL/Redis明确跳过，等待 CI 执行。
- Redis 门禁集成测试：已编译；本机明确跳过，等待 CI Redis 7 执行。
- 七类供应商失败分类、发送失败验证码不可用、功能关闭不调用 Sender、邮箱链路隔离：通过。
- 未绑定、场景停用、模板未审核、模板本地停用、签名/供应商不一致、模板编码为空和变量不匹配均在 Sender 前失败关闭：通过。
- 同一验证码并发消费、同一幂等键并发测试发送：用例已纳入 CI race 路径。
- 单 IP 每分钟十次/第十一次 429 的真实 Redis 行为用例已纳入 CI，并轮换伪造 XFF/X-Real-IP 验证不能拆桶；
  手机号+场景并发发码 16 路仅一次放行。
- 手机公开发码与密码重置均有路由级来源失败前置测试，证明可信代理异常或解析器缺失时不会进入 Redis 或业务服务。

### 2.2 前端

- 用户控制台短信/隐私测试：9/9 通过。
- 用户控制台邮件回归：15/15 通过。
- 管理后台短信契约：9/9 通过。
- 管理员 MFA 契约：8/8 通过，包含手机四字段受理校验。
- 管理后台邮件回归：11/11 通过。
- 管理后台 Vite 环境隔离：1/1 通过；用户控制台 Vite 环境隔离：1/1 通过。
- 两个控制台 TypeScript、ESLint、生产构建：通过。
- 上述短信、MFA、邮件回归、环境隔离和出站 URL 契约测试已显式接入 PR CI，不再只依赖本地人工执行。

构建存在既有大 chunk 与动态/静态混合导入 warning，不影响本阶段通过；依赖审计仍有仓库既有
moderate/high 项，作为残余供应链风险记录，不在本阶段无关升级中扩散修改。

### 2.3 浏览器行为

使用真实 Chromium 和本地 Vite，API 由浏览器路由 Mock：

- 用户端：注册、手机号登录、找回密码、换绑手机号入口；成功发码进入倒计时且按钮禁用；429 在表单内持续提示；
  个人中心只显示脱敏手机号。
- 管理端：模板列表、详情、五场景绑定、发送日志、测试提交弹窗、四权限显示；normal、loading、empty、error、
  forbidden 五态；1440、1024、768、390 宽度均可操作。
- 测试弹窗只打开未提交，未发送短信。
- 浏览器临时证据含测试号码和临时令牌，因此验收结束后已删除，不进入 Git、报告或截图。

## 3. 五场景证据

| 场景 | 单元/Mock | HTTP 路由 | 验证码消费 | 最终状态 | 隔离 MySQL/Redis |
|---|---|---|---|---|---|
| register | 已通过 | CI 用例已实现 | 一次性消费断言已实现 | 用户创建断言已实现 | 待 CI |
| login | 已通过 | CI 用例已实现 | 一次性消费断言已实现 | 会话/令牌断言已实现 | 待 CI |
| reset_password | 已通过 | CI 用例已实现 | 一次性消费断言已实现 | 密码和会话断言已实现 | 待 CI |
| bind_phone | 已通过 | CI 用例已实现 | 一次性消费断言已实现 | 手机/MFA 状态断言已实现 | 待 CI |
| admin_verify | 已通过 | CI 用例已实现 | 一次性消费断言已实现 | 手机 MFA 断言已实现 | 待 CI |

CI 五场景 HTTP 用例还覆盖：每场景错误码、成功、业务入口重放拒绝；换绑未登录 401；管理员缺权限 403；
重复公开发码 429；登录账号脱敏落库；三类敏感审计无摘要。过期边界由现有真实仓储 SQL/边界测试覆盖，
隔离数据库执行状态仍以 CI 为准。

登录日志专项单测覆盖标准手机号、异常短手机号、长/短邮箱、本地部分缺失、无 `@` 和未知登录类型；异常输入
统一失败关闭为不可识别占位。历史部署库记录未在本阶段修改。

## 4. 未验证项

- CI 尚未运行，因此真实 MySQL 8、Redis 7 和 Linux `go test -race` 不能标记通过。
- 未部署阶段 4 提交。
- 未连接阿里云、未发生真实受理或真实收件。
- 未获真实短信授权；如后续确有需要，必须另行提交受控验证方案。

当前准确结论：**阶段 4 本地自动化与本地 Mock 浏览器验收通过；隔离数据库、Redis、race、PR/CI 和真实短信部署态验证尚未完成。**

独立规格、标准/安全、QA 和产品经理均完成本地复核：P0=0、P1=0；两个 P2 已由产品经理书面拆分，
一个 P3 已接受后续增强。测试工程师的正式签字仍以 PR CI 的 MySQL 8、Redis 7 和 Linux race 全绿为前提。
