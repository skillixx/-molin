# VID-G5 交付与读取对账：独立切片核查

测试工程师`vid_g5_cancel_slice_qa`只读核查源码与测试设计，不修改文件、不执行数据库或Provider。主任务执行一次性MySQL验证。本回执不是全阶段QA/PM/Standards/Spec验收。

| 发现 | 局部级别 | 复现与关闭 |
|---|---|---|
| G5-RECON-001 | P1 | 相反财务事件被忽略，已真实复现；现按统一三轴矩阵逐条核验并对齐当前状态 |
| G5-RECON-002 | P1 | 额外执行Attempt漏检，已真实复现；提交前与最终对账均拒绝额外尝试 |
| G5-RECON-003 | P2 | 同Task但Provider错绑回调漏检，已真实复现；现校验关联Provider二元标识 |
| G5-READ-001 | P1 | 旧构造器跳过G5门禁，已真实复现；现拒绝任何非NULL新协议身份，避免排序规则别名降级 |
| G5-READ-002 | P1 | 读取用入口旧时钟可跨期放行，已真实复现；事务返回前刷新时钟检查已锁定资产 |

核查确认修复对应原发现；发布标记绑定请求目标版本和当前租约，升围栏但不加attempt，重领清除旧标记。发布、六资产available、completed与最终对账处于同一外层事务；外部读取仍要求无活动补偿和最终检查通过。

最终隔离运行：`ok molin/server/internal/modules/token_gateway/service 92.608s`。正常T2V/I2V各100并发、补偿发布/重放、纯交付失败恢复、11个发布故障点、末尾媒体/租约过期、子资产保全及旧G4事实共存均通过。本机Go/vet/mod verify、gofmt、diff检查和敏感扫描通过。

仍未完成其他结果矩阵、合法调账及钱包动作闭合、所有旧基础设施回归和完整G5最终验收。不把本回执解释为全阶段P0/P1/P2=0。当前LOCAL_ONLY，真实Provider、Key、钱包、费用与部署操作均为0。
