# VID-G6 平台资产删除局部独立复核

产品角色`vid_g6_g5_gate`依据综合多模态规划§24.6父删子联动规则，确认content根联动普通子资产、普通子资产仅删自身、审核副本不可普通删除、长期副本保留。单删意图即阻止兼容交付，v1隐藏不表示根正文被删除；确认后才显示平台部分删除标记，原执行/计费/交付事实不改，不新增价格或保留政策。

独立QA角色`vid_g6_contract_audit`发现P1：通用metadata摘要不含内部存储位置，原见证校验没有逐项绑定plan.ref/hash/size；校验A后可能删除B。主代理46196真实反例failed=false且Delete=1，发生于本地Fake对象，不涉及真实数据。

修复同时补SQL绑定与运行时字段校验。主代理23524的5项专项exit0/Linux race/schema92，service22.755秒，copy=`e3cc7ddd7b75a57fa43eccac685d463aed56fb2238841261ec005e5a59e27e8d`。写入篡改被SQL拒绝；另外先形成合法delete_failed，再只篡改见证Query返回值并重算摘要，要求精确ErrVideoMediaProtected、Delete增量0、所有原正文及真实见证和财务不变，此项通过。

独立QA结合此前源码审查、本轮只读核对和主代理动态证据确认P1可局部关闭；QA未另行运行数据库，也因自身角色范围未修改server文件。反例及修复均由主代理落盘和执行。

父/兄弟/审核副本保留、单删后根组收敛、原键重放、版本/归属拒绝、部分删除投影和长期副本继续读取已有基础HTTP证据。完整并发、故障、JWT/I2V、COMMIT未知及整个G6验收仍未完成，不据此宣称生产或商业可用。
