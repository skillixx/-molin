# VID-G5 追加调账基础路径独立核查

本文件记录局部开发核查，不是完整G5验收。

- 基线：`36b6a5c5f9e60a4ef182ae434337bb05e165477c`。
- 源码检查点：`da22d30db5c30e19a8b0979a3aa0bcfdef32ccefa03ff2bb45e479908f9d79e1`，70个源码/脚本文件，67个Go文件。
- 独立任务：`vid_g5_cancel_slice_qa`，只读源码与合同，不执行数据库、Docker或Provider。
- 运行验证由主任务执行，结果见[调账检查点](./video-gateway-vid-g5-adjustment-checkpoint.json)。

## 合同核查

独立核查确认，保持原settled/released、原结算金额、sale_line和cost_line不变，只追加独立钱包动作及adjustment，符合已批准F5合同。调账不应成为重计费或第二次Provider提交。

核查提出“仅唯一外键不足以证明新增资金动作”：必须禁止借用原冻结/消费/解冻。实现已加入全请求原钱包关联集合互斥，以及同用户、钱包、金额、方向、类型、序号标识与流水顺序检查。新增反例使用原消费流水及完全相同金额冒充调账。

当前源码未发现确定P1/P2；这不等于完整阶段缺陷计数为0，也不替代运行结果或产品确认。

## 已建立的运行用例

- 独立金额金样：取消返还后10元，调增0.25后10.25，调减0.10后10.15；原请求消费仍为0。
- 同一调整序号100并发只追加一次；同序号不同金额冲突。
- 钱包CAS、流水、Usage、Outbox四个写点失败均整体回滚。
- 仅有Adjustment而无对应钱包动作，对账保持失败。
- T2V/I2V成功结算并交付后调增0.10，分别为9.60和9.35；原价和确认成本仍独立核对。
- 原消费不能冒充新调整；被关联资金流水和Usage不可改删。
- 历史checker停用不使旧事实失效，新调整仍要求有效主体。
- 100个不同序号各扣0.2元竞争10元，最多50笔成功，其他余额不足；冻结额不变。

首个合法调整在占位实现上先红（0.396秒），初始两组在实现后定向通过（11.794秒）。新增用例和全量已实现G5测试的实际结果以检查点为准。

主任务最终默认all隔离MySQL/race通过（296.912秒），含完整1..77迁移、重复up、保留式down/re-up；同一源码全量Go测试、vet、依赖检查、gofmt和敏感扫描通过。这个运行结果覆盖上方已建立用例，未覆盖下方剩余验收项。

## 未结束的验收范围

独立核查要求继续补齐入账溢出、跨用户/钱包引用、重复资金引用、错误Outbox、行数齐全但NULL资金关联等反例；完整参数和归属矩阵尚未全验收。

完整G5仍需跨门面幂等、12金额金样、Chat/Image隔离兼容、Python证据校验及最终QA/PM/Standards/Spec。同一财务合同的人工批准不扩大为真实资金或Git授权。本轮无真实Provider、真实钱包、真实资金、测试服或生产操作，未提交、未推送、未进入VID-G6。
