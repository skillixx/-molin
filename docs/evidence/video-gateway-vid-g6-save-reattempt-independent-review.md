# VID-G6 多尝试保存局部独立审查

独立QA角色`vid_g6_contract_audit`只读审查migration91、尝试选择、保存完成、清理、媒体删除及内容故障测试，未修改代码或另行运行数据库。

目前未发现确定的旧键漂移、误删或误释放路径：命令绑定精确save_public_id，finish/cleanup/CAS均定位public_id；新尝试检查历史连续前驱与aborted清理证明；live_task_id唯一约束限制一个有效尝试；媒体删除扫描全部历史，不因旧aborted漏掉新copying。

迁移顺序先回填命令，再解除Task主键。回填只允许原缺失引用补齐且保持其它字段；孤儿、错Key、多候选拒绝。down明确旧版本回滚需要同时关闭保存、媒体DELETE和清理，不能只关闭保存。空库成功不等于历史数据迁移验收，必须继续补历史行/命令、DDL中断重入、错误前驱/Key和孤儿拒绝测试。

内容故障回归46587失败后，49021定向证明7个子例均未真正注入，err均为nil。独立角色确认固定Store是正确的生产边界，不应为测试动态换后端。修复在创建cap前装配固定指针wrapper，atomic mode和hits==1证明故障实际命中；保留原cap及撤权、对账损坏、取消、范围边界断言。

主代理实际动态证据：39166基础4项exit0，schema91；88532增强6项exit0/Linux race，service59.418秒，copy=`33fa4bd0235f101cd84291391429efc0b7ff0d395278f7f64505bea8ec0db2cb`。100新键为同一App上的并发，证明唯一后继/资产/一次容量/五次复制，不冒称跨进程实测；7种存储故障分别命中并拒绝。以上动态证据不属于独立角色另外执行。

本回执不签迁移完整验收，也不签完整保存或VID-G6。历史迁移、更多竞争/恢复、平台HTTP终止回执、完整管理/回调/SDK/兼容回归及Git门禁仍待完成。真实Provider、Key、资金、共享服务器和生产均未操作，不进入G7。

## 冻结后复核

独立角色已逐文件复算201文件及SOURCE一致：`dab68c71ff4e4660d27ed3bc6bbd3b1e2765df0081fbaf860dbc437489a3e59e`，清单为`video-gateway-vid-g6-save-reattempt-source.json`。结合49021红测及88532绿测，确认内容故障夹具问题可局部关闭；没有另行运行动态测试。11387当前99项回归尚未结束，不记为PASS。历史迁移与整个G6仍未验收。
