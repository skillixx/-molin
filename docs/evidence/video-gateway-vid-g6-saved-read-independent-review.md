# VID-G6 长期读取局部独立复核

本回执仅关闭G6-SAVED-READ-001/002/003三个P2，不是整个长期读取或VID-G6验收。源码仍未提交，BASE为`52563ba450c6d488456137162580022deb06acc8`。

## 精确证据

独立测试角色`vid_g6_contract_audit`只读审查并复算196文件全部一致：SOURCE_STATE_ID=`2fa12c6e53cd5cfc01e522bf21a700bf434d51a01cc11dd4319c5e3ef088004c`，清单为`video-gateway-vid-g6-saved-read-source.json`。

主代理实际执行9026：临时MySQL、schema90、Linux race，12个必跑顶层测试全部RUN/PASS、无SKIP、整体exit0，service84.634秒。运行器copy hash=`2f71ac3d292bced7dc01c9437da5180ca4efc42c087ff63667e806b7f48ff4f3`与冻结快照一致。独立角色引用这些动态工具回执，没有另跑数据库测试。

## 已关闭三项

- 001：44560真实第三路限流反例仍访问Head。现在数据库保存证明与外部五对象确认分开，当前权益、签名、共享下载名额先于Store。9026共享名额和HTTP错误签名/跨角色/权益拒绝零Store通过；五副本证明仍完整。
- 002：91884在发布前最后一次权益查询对齐650ms，不改变字段或SQL模式，实际读回StartedAt超前Save返回327ms。写入即时StartedAt取秒后，9026确认立即签发可用，真正未来时间仍拒绝。早期“保存准入失败”的误定位已纠正为保存后签发失败，错误位置诊断已移除。
- 003：40252分别在保存UserAsset、权益关联与权益实体读取注入数据库连接错误，三项错误被误报为业务拒绝。修复区分记录不存在与数据库依赖故障，9026三项实际命中注入、返回不可用且零Store通过。原六资产树的数据库故障同样不混为保护冲突。

## 相关覆盖与限制

9026还覆盖原临时源自然到期及合法DELETE后五角色长期字节/hash/MIME，v1删除后404；独立Store及临时影子不能掩盖长期丢失；新保存策略不撤销旧冻结资格；19张业务/保存/容量表整行不变，排除允许变化的认证使用时间、下载租约及范围锁；JWT正常读取与吊销；真实4MiB HTTP首片后JWT撤销、存储权益撤销和自然到期均只返回首片并截断；SQL冻结类型含大小写与NULL变更拒绝。

独立产品角色`vid_g6_g5_gate`确认原存储资格、原Key、冻结类型、旧NULL不伪回填、不改变原财务和保留期限的方向；指出并修正“自然到期即v1 404”的过度文案。当前只承诺合法DELETE后的404，纯到期Job行为须按原合同另外验收。

旧NULL兼容强证据、完整元数据漂移/跨主体矩阵、保存重试与更多竞争，以及完整管理/回调/SDK/兼容/Git门禁仍未完成。97项当前G6回归46587已启动，未结束不能算通过。真实Provider、Key、钱包写入、费用、共享服务器、生产和G7操作为0。
