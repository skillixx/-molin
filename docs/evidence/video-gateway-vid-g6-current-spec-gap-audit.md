# VID-G6 历史规格缺口盘点（已由终审整改取代）

本记录承接早期独立规格核对角色 `vid_g6_contract_audit` 的只读结果，是保留的历史快照，不再描述当前实现状态，也不是完整阶段验收通过。其后inline I2V、模型目录/发布、Project Key、预算、queued/running、下载及管理写矩阵均已进入终审整改；当前状态以`video-gateway-vid-g6-review-remediation-progress.json`、最终requirements和acceptance为准。基线仍为 `52563ba450c6d488456137162580022deb06acc8`。

## 仍需补齐的实现

1. `/v1/videos` multipart 图生视频仍需进入既有 UploadSession/InputAsset，覆盖失败、断连、重放和孤儿清理。
2. 公共模型目录缺少明确的 `capability`、`supported_operations`；后台七键视频配置尚未接通编辑/发布流程，不能用内部解析器代替实际接口交付。
3. API Key 已有默认关闭的视频能力数据库位，但缺显式管理与回显链。
4. 生成预算、并发、排队尚未在钱包 Hold 前接入。已有治理仓储可复用；上传/下载限额不能替代生成限额。

## 已实现但仍需补验收，不得重复重建

输入保留清理、保存清理、迟到复制围栏、COMMIT未知、cleanup后新尝试、长期读取、隔离/解除、poll、archive-retry、调账均有源码和局部动态证据。主文档内旧的“未实现”描述需要纠正，但局部证据不能扩张为全量PASS。

## 尚未闭合的验证

- 锁定 Python 2.45.0 / TypeScript 6.39.0 SDK 的真实认证 HTTP 互操作；当前静态回执仍标记 `real_http_contract=NOT_RUN`。
- 管理员各接口完整负向、并发、写入故障、过期权限和提交未知矩阵。
- 实际默认应用关闭视频、缺依赖不装配Fake的统一证据。
- 媒体、财务、普通审计、安全及争议证据的差异化保留测试映射。
- 最终同一精确源码的Go全量、vet、mod、Linux race、敏感扫描、Chat/Image/G0—G5回归及独立角色门禁。
- 本阶段唯一PR的Ready完整CI、普通合并及main包含性证明。

## 历史证据边界

独立盘点时最新调账清单321文件无漂移；旧保存清理184文件清单有36文件变化（28个源码/运行脚本），旧输入清理110文件清单有24文件变化（17个源码/运行脚本）。历史结果保留，不覆盖，也不用于证明当前全量源码通过。

本历史盘点当时误记46条；当前权威清单为原43条明确路由加4条支撑路由，共47条。bootstrap未注册视频符合原Goal默认关闭方案，不要求本阶段部署。盘点未运行数据库，不签QA或完整Spec PASS。
