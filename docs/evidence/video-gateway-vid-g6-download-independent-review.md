# VID-G6 下载限制独立增量复核

本回执不是完整G6验收。独立角色为产品经理`vid_g6_g5_gate`与测试工程师`vid_g6_contract_audit`；动态运行由主代理执行，不冒充独立角色亲自运行。

产品角色确认MySQL操作性租约不改变G0用户2/Project4与20MiB/s合同，60秒TTL属可逆工程选择，不是媒体保留政策；必须原子取得两层名额、按原持有者CAS续约/释放，不修改Task、媒体或财务事实。1MiB突发不能逐片重置，写deadline须受租约约束。

测试角色发现P1 G6-DOWNLOAD-001：只锁租约行续期时，续约未提交跨旧TTL可让新申请误补第三个名额。77066旧实现固定窗口真实失败。修复后申请与续约共用User→Project范围锁；续约随后才做有效期/version CAS，且不再获取Task/钱包锁，未发现新增确定锁环。59779增强窗口通过，15246再次通过；该P1按本子项关闭，不等于全阶段P1清零。

增强测试要求UPDATE确实影响1行、同事务连接ID、数据库UTC跨期限，以及绑定schema/engine/阻塞连接的真实scope行锁等待；结束后有效租约恰好2。两App对象、两Task的100申请增加覆盖，但不声称多进程部署，也不以其代替固定窗口。

独立角色复算原121文件SOURCE为`0f94beffd6822d43dc4b3fe2a9937a0952b193168a1516fbc0ffe09327a132f1`。随后指出active=0不足以证明过期记录已显式释放，且客户端收完正文早于Handler清理可能造成竞态。仅修改`video_content_http_mysql_test.go`：测试服务端在mux返回后按Request ID发送完成信号，GET有界等待，再断言包括已过期记录的released_at IS NULL总数为0。

独立复核确认新断言没有时序缺口或假回收，当前顺序GET可安全跳过未消费POST信号；将来并发化需按ID分发。该文件SHA256为`a64d72f4307e8cbf342b15eeb6a30ec3c4ca354fa0d7bd23f5a0fb8fed3df76f`，当前SOURCE复算为`25d71549f6cba032efad983585f260cfbc2f4d4e6e0baefb84d1ebb128877872`。

主代理动态回填：24652在前一快照通过47项必选测试；当前15246下载专项通过，含增强回收断言；79808全Go/vet/mod通过。详见下载检查点。Project第五路、真实慢TCP超时、大对象业务断流、COMMIT未知、完整财务不变、SDK/browser及全阶段剩余接口仍未完成。
