# VID-G6 管理只读详情独立复核（局部）

产品角色`vid_g6_g5_gate`核对既有Image管理链及Auth源码：管理员JWT、管理权限与手机/邮箱MFA共同决定资格，不自动新增直接admin角色门槛。MFA使用users既有两个时间戳、默认24小时配置，0保留不过期语义，缺失/未来/过期失败关闭。GET不要求reason/CAS或产生审批事实；目标用户/Key停用不应遮蔽已有管理记录。

独立QA角色`vid_g6_contract_audit`确认目标JOIN和原Task→Request→Quote/Link/Hold/资产读取顺序合理，并指出服务层credential未绑定UserID、MFA查询故障被bool折叠。主代理通过21420实际复现两者；此前18312还复现GORM链污染导致权限拒绝误503。

修复在原权威链内完成：保留干净事务查询基准；JWT私有凭据冻结claims.UserID并精确复验；Auth新增共用规则的CheckAdminVerified(bool,error)，旧IsAdminVerified继续错误返回false，新管理入口按错误503与未认证403/40031分流。未修改短信/邮箱发送、MFA保留期、价格或钱包规则。

17505两项与65790四项隔离测试通过。65790为schema93/Linux race，service5.887秒，复制树`13866ad80cacee3bf1583a07080d9715fa9d8d1d9012a3d48f1225ea78d348b6`。测试实际检查reserved/completed、目标禁用可见、28字段、业务事实快照与零Store/Provider调用、主体替换失败、MFA SQL错误及bool兼容。QA据源码和主代理记录确认三项已复现缺陷可局部关闭，未另跑数据库；这不等同完整管理面或G6验收。

QA还指出原快照断言位于最后两个故障反例之前。主代理已补末尾再次比较完整业务快照及调用计数；此补强和后续文档变化不能复用65790为最新源码通过证明，仍须扩大回归。末尾撤权/跨期及其余11路由未验收。

新增末尾无写入断言已纳入30405的123项回归，尚待运行结果；65790证明的是此前快照。QA再次复算239文件清单`d908cef17a3c4fcb99c06cf1f82a3c682c2219f03f553c650561174f2832385f`，逐文件无漂移，并确认补强位置及文档未缩小剩余范围；未提前签30405通过。

回调额外HTTP负向23688曾复现未知内容参数与gzip声明被接受；拒绝未支持声明后91657九项通过，另覆盖跨Task nonce竞争一胜一409与失败者无遗留Callback。回调I2V及其他完整矩阵仍保留。
