# VID-G5：Chat/Image兼容局部复核

## 边界与结论

独立复核任务：`vid_g5_cancel_slice_qa`。对象为未提交的G5本地工作树，基线`36b6a5c5f9e60a4ef182ae434337bb05e165477c`。这是源码复核与主代理执行记录，不是独立完整G5验收，也不代表生产或商业可用。

本次实际MySQL运行仅使用临时内部Docker网络、无宿主端口、现有镜像及合成钱包；旧Chat账务用例中的HTTP仅为本地`httptest` Stub。没有外部Provider、真实Key、真实钱包、RabbitMQ、Redis、MinIO或远程服务器操作。既有共享容器未被启动、停止或改动。

## 发现与关闭

| 问题 | 复现与处理 | 验证口径 |
|---|---|---|
| G5-COMPAT-001：旧Image69/70/71/G6/G7脚本缺77 | 五个脚本在Go运行前安装77；69/70补齐此前71前置链；历史迁移回退断言保留 | 静态测试先红后绿；69/70/71/G6实际隔离运行通过；G7只运行清理Repository，不运行基础设施段 |
| G5-COMPAT-002：旧Chat fixture假定wallet主键等于user_id | 先共存一个视频Hold，原固定钱包ID触发重复主键；改为自动主键，仅用user_id关联 | 隔离回归证明旧Chat不改动已有G5钱包 |
| G5-COMPAT-003：旧成本断言固定采用另一套夹具价格 | 完整迁移启用G3原先跳过的成本分支；5/10成本价对应0.00010000，G4的1/1对应0.000015 | 只接受两套固定百万分母及字面金样；未知价格失败，不调用业务计算器生成预期 |
| G5-COMPAT-004：Chat对账扫描/人工终结误读视频 | 全G5与Chat共存回归复现；扫描、单项恢复、锁定终结、重试前序读取均限定chat/chat.completions | 超时扫描必须不领取视频，人工终结必须拒绝，视频钱包保持不变 |
| G5-COMPAT-005：Chat预占入口可接受非Chat | 八个Prepare/Quoted/Retry入口反例；四个公开入口及内部事务先统一校验 | 显式image/video及非空operation拒绝；旧调用省略字段保持Chat默认；不留下请求/Hold |

独立复核确认未发现上述修复遗漏的公开写入口，固定价格断言未被削弱。最新Chat专用隔离运行包含原31个子测试及八个非Chat入口反例，`go test -race`返回PASS（4.312秒，执行会话63020）；完整阶段MySQL和源码摘要见配套检查点，不能用本结果替代全部业务兼容。

## 未运行与待补范围

- 旧G7 Image基础设施、RabbitMQ/Redis/MinIO运行段保持NOT_RUN，符合阶段OFF约束。脚本静态兼容不等于基础设施运行证明。
- 仍须独立临时MySQL补跑旧Chat G4预算、G5管理Repository、G6用户Repository/Service、G7可靠性。普通`go test ./...`的环境跳过不是它们的执行证据。
- G4预算清理包含无WHERE的补偿表/预算表删除，只能在没有G5事实的独立新夹具库运行；不得与当前G5 all库混用。
- G7可靠性有固定ID与全库积压断言，也需独立夹具库。
- 本轮曾在一个Shell脚本运行期间改动脚本，造成读取偏移与退出127；没有记录为通过，随后冻结脚本并从新临时资源重跑。其失败不属于业务实现缺陷，亦不抹除历史失败。

所有变化仅本地未提交，G5最终QA/PM/Standards/Spec仍待完成；不进入G6。
