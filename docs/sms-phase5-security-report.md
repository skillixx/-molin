# 阿里云短信验证码阶段 5 安全报告

## 1. 已完成控制

- 开关默认关闭，测试模式默认开启，模板编码不进入环境变量。
- 指标仅使用五场景和八个安全结果类别，不含手机号、模板编码、业务请求号、供应商原始码、OTP 或凭据。
- 指标写入端拒绝非固定场景，并把未知错误类别收敛为 `rejected`，避免异常字符串形成常驻高基数键。
- 供应商超时、限流、签名、模板、欠费、网络和普通拒绝分别归入固定低基数类别。
- Nginx 仓库配置和测试服实际配置均覆盖 X-Real-IP、删除 XFF/Forwarded。
- 回滚 Dry Run 不连接远端、不写配置、不重启、不执行 migration、不发短信。
- 指标聚合增加并发读写测试；Linux CI 使用 `go test -race` 验证互斥保护，并独立执行 Nginx、promtool 与回滚门禁。
- 环境文件准备度检查锁定阿里云供应商和官方 Endpoint，校验独立 HMAC 最小长度，并拒绝重新引入模板编码环境变量。
- 测试服已使用独立 `/28` 固定代理网络；运行环境中的两个旧 `SMS_TEMPLATE_CODE_*` 覆盖项已移除，数据库绑定保持唯一真相源。
- 关闭态双代理请求均为 `503/50300`；发送日志、手机验证码行、Provider 指标和完整白名单号码日志均无增量。
- 部署后只读脚本通过 stdin 向 curl 和容器内 mysql 传递内部 Token/数据库密码，敏感值不进入宿主机命令参数。
- 测试服管理代理上的 5 个短信管理 GET 已使用现有合格管理员只读验证；响应递归检查未发现 AccessKey、Token、
  OTP、完整手机号或供应商原始敏感字段，4 个管理写接口均未调用。
- 鉴权负例使用现有活跃账号，只读选择无权限和 MFA 失效候选；临时 JWT 有效期 5 分钟，仅在内存和 stdin 中流转，
  不输出、不写文件、不修改账号。请求前后短信日志、验证码、审计和 Provider 指标均无增量。
- 测试服页面复验使用运行时已验证为 summary 200 的合格管理员候选；10 分钟 Token 仅进入隔离 Chrome 上下文，
  不进入命令参数、输出、DOM、截图或文件，关闭上下文后销毁。浏览器只产生短信 GET，写请求为 0。
- 测试服 Alertmanager 修正版单次演练只执行 1 次 firing 和 1 次 resolved，均人工确认实际收件；结束后恢复 `discard`，
  Provider 与真实短信增量为 0。journald `8G/50G/14day/1day` 已受控部署并通过独立只读运行时验证。
- 双号码本地隐藏输入候选绑定脱敏计划 ChangeId 与摘要，仅实现隐藏输入、BSTR 临时解包及内存校验；经独立授权实际提示
  2 次，格式与互异性通过。号码未输出或持久化，网络、上传、白名单修改、短信开关修改和真实发送均为 0；归属、注册状态、
  管理员身份和白名单状态仍保持未验证。
- 新增阶段 5 只读敏感信息门禁：比较阶段分支与指定主分支基线，扫描分支引入的全部历史 blob 与提交说明、未提交工作树、
  两套前端构建产物，并独立拒绝 Git 跟踪或历史中出现的 `.env/.env.local/.env.test/.env.production`。门禁只输出分类、
  相对路径、行号和 Git 对象摘要，不回显命中的手机号、验证码或凭据。
- 2026-08-05 加强后本地复验覆盖当前工作树/构建产物 159 个文本文件、56 个阶段改动路径、295 个历史路径版本、
  44 条提交说明，两套 `dist` 均验证存在；结果为 0 发现、短信启用赋值 0、真实发送 0。21 个公开命令行契约用例覆盖
  安全关闭态、Go 运行时布尔真值表、JSON/flow YAML/PowerShell/命令前缀、历史删除逃逸、复用基线 blob、Git index 与工作树分叉、
  合并冲突解决 blob、禁止模式去误报、未提交工作树、文档/测试手机号与 OTP、裸 AccessKey/Bearer（包括占位词子串及 Base64
  符号）、NUL 文本、手机号/OTP/Unicode 行段分隔符等敏感路径脱敏、受保护环境文件、缺失构建产物和敏感产物脱敏拒绝，均已通过。

## 2. 当前风险

1. 测试服真实短信 Canary、五次供应商受理、OTP 未消费、五场景人工收件及 5m/15m/30m/2h/24h 五档观察均已通过；最终关闭态和五窗口证据已离线组装并通过权威验证。该结论仅覆盖测试服关闭态观察，不能替代生产验收。
   2026-08-07 的独立补充 2h 只读观察在距 Canary 完成 75594 秒时仍保持累计发送 `21/20/1`、当前进程 Provider `0/0`、零活动告警和零通知失败增量，且没有配置、服务、业务 POST、邮件或短信副作用；该结果不属于五档权威序列，也不替代 24h 门禁。
   原 24h runner 的一次性执行因检测到远端 stderr 非空而失败关闭，未创建快照；远端退出码 0 不能替代本地完整门禁。修正版继续把任何 stderr 视为失败，只补齐白名单低敏失败摘要且不输出 stderr 正文，须使用新 ChangeId 和新摘要重新批准。
   修正版执行进一步确认全部 24h 观察字段和零副作用门禁通过，唯一失败项仍是 SSH stderr；传输参数现已补充 `-T` 和零口令提示，以消除非终端 stdin 的伪终端警告，同时保持任何剩余 stderr 必须失败。第三版尚未取得执行授权。
   第三版显式 `-T` 后 stderr 仍存在，故伪终端推断未确认且快照继续不生成。下一候选改用 `env -i`、固定 locale 和 `bash --noprofile --norc`，失败证据仅含固定分类、字节/行数和 SHA-256；不输出正文、不接受 stderr，也不降低观察门禁。
   隔离环境版执行后，全部 24h 业务观察字段仍通过，但 SSH stderr 仍为 1 行 57 字节，分类 `other`、SHA-256 `1be4aae086b93109187f5c53d6269e4a399e6cdb039e41389362ee63c6677076`。原始正文未输出且不能从摘要安全反推，因此不得豁免。仅远端执行 `/usr/bin/true` 的传输层诊断候选已离线生成并通过默认关闭、脱敏、重放锁和敏感扫描；最终 ChangeId `20260807T071436Z`、runner SHA-256 `407b57e301c30e987abbba83fe8512b925e6dddd095a0f07ecc688fadd18123d`，须独立授权后才能连接一次测试服。
   上述传输层诊断已单次执行并返回空 stderr，结果 SHA-256 `9a17aec9c374b5ea334448f2bd062c5be25495aea52ba11fc97c54e2865c4b14`，执行锁已保留且零业务读取、零配置/服务操作、零消息发送。基础 SSH 层据此排除；下一诊断边界收窄为观察 runner 的 `env -i`、locale 和无启动文件 Bash 包装，仍不得读取业务状态或复用已消费 ChangeId。
   隔离 Bash 差分候选 ChangeId `20260807T072327Z`、runner SHA-256 `7fe114dd12236d47dc2a9111967f127ef8a37be9bd2b853a05997b7b4b56b4d7` 已通过默认关闭、一次性锁、脱敏、专项契约、readiness 和敏感扫描。它只执行隔离包装后的 `/usr/bin/true`，没有业务 payload；尚未联网，须独立精确授权。
   该候选已单次执行且 stderr 为空，结果 SHA-256 `cecb38090f9b03c26f1a852480426d14cca800b5a6aca68304acb741811e2ca2`，执行锁已保留。隔离包装据此排除；下一候选只能验证 `bash -s --` 加一行 `true` 的 stdin 差异，不得包含业务 payload、数据库或 HTTP 读取。
   stdin 模式候选 ChangeId `20260807T073038Z`、runner SHA-256 `2c55627ead5ef550c1f5a8c9a5fe87398f4bb04ab9324d1b6a2f576d2dd34e9b` 已通过默认关闭、一次性锁、5 项专项契约和敏感扫描；stdin 仅为 Base64 冻结的 `true\n`，尚未联网，须独立精确授权。
2. 生产目标、网络、密钥注入、监控和回滚点尚未只读审计；API/前端制品交付、migration 执行器、版本化备份及服务控制的实际拓扑也尚未由项目负责人冻结，禁止从测试服推断生产执行命令。
3. 前端依赖存在阶段 4 已记录的既有供应链风险，本阶段不做无关升级。
4. `verification_codes.target_value` 的历史手机号兼容列仍按阶段 4 产品决定保留；HMAC 化需独立 migration 和停发窗口。
5. 测试服 journald 已配置 `8G/50G/14day/1day` 并通过运行时验证；24 小时观察已完成且未出现监控或通知异常。长期容量趋势仍应由后续运维持续观察。
6. 测试服真实页面存在 1 条未能关联响应路径的静态资源 404 控制台提示；功能性控制台错误、页面异常和业务 API
   非 200 均为 0。该观察不影响短信安全门禁，但正式发布前宜由前端/运维继续定位静态资源来源。

7. stdin 模式候选 ChangeId `20260807T073038Z` 已按授权单次执行并以退出码 127 失败关闭；1 行 58 字节脱敏 stderr 明确显示 UTF-8 BOM 被加在 `true` 前，结果 SHA-256 为 `adbbbc45a066e93d095e03e47222a529e2566211ae2213776df9c31ad8004069`。离线重算与历史 57 字节摘要完全一致，确认原观察 runner 的 `set` 首行同样被 BOM 污染。修复不豁免 stderr，而是移除 Windows PowerShell 5.1 的流式 stdin 写入，改用受限临时文件重定向和逐字节复核；新候选 ChangeId `20260807T081228Z`、runner SHA-256 `cb7a5611c9382a026460a5e7a276f6732abb7318539012ecae919e5e6fbb7226` 仅完成本地静态验证，尚未联网。

## 3. 安全停止条件

发现完整手机号、验证码、AccessKey、JWT、Bearer Token、供应商原始响应或签名请求原文进入日志、指标、审计、
文档、截图或 Git 时，立即停止发布，关闭开关，保留脱敏调查证据并按密钥轮换流程处理。
