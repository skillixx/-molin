# 阿里云短信验证码阶段 2 本地测试报告

## 结论

截至当前阶段 2 工作树，本地后端自动化、安全扫描以及 PR #315 预合并修复提交 `9686433` 的 MySQL 8、Redis 7、Linux race 远程 CI 已通过。隔离测试服已部署提交 `e637da7`、执行 migration `000059`，模板同步重复执行结果为 0 新增/0 更新/1 未变化，五个场景均已绑定同一已审核且启用模板。受控真实短信因管理员 259 原绑定手机号不是真实可收件号码而安全停止，未调用 SendSms；正式验收仍未形成证据。当前状态只能表述为“阶段 2 代码开发及自动化验证已形成，完整阶段验收未通过”，不能表述为生产可用或阶段 2 已完成。后续提交必须以 PR 当前三项检查全绿为合并前证据。

## 已执行

| 检查 | 结果 |
|---|---|
| 阿里云模板列表 + 新版详情适配器单测 | 通过 |
| 同步失败零写入、固定签名/验证码模板过滤 | 通过 |
| 模板概览、启停 CAS、场景固定集合与九路由权限 | 通过 |
| 测试发送关闭态、首次提交、accepted 幂等重放 | 通过 |
| 16 路并发幂等重放（连续 20 轮）仅一次限流和一次供应商调用 | 通过 |
| 同键异参冲突、跨管理员幂等隔离、白名单前置拒绝 | 通过 |
| 管理员/手机号 HMAC 双维 Redis Lua 限流及失败关闭单测 | 通过（测试替身，不等于真实 Redis） |
| 真实 Redis 双维原子计数、每分钟第 11 次拒绝及 TTL 集成资产 | 本机安全跳过；GitHub Actions 运行 #373 的 Redis 7 门禁通过 |
| 两路同版本场景并发更新仅一路成功 | 通过 |
| 20 路模板同步去重、畸形快照零写入、重复模板忽略 | 通过 |
| 发送日志隐藏 `pending`、Phone HMAC 和内部摘要 | 通过 |
| 九接口未登录、缺权限、缺双重认证矩阵 | 通过 |
| 写操作成功/失败审计摘要脱敏、列表筛选和时间范围校验 | 通过 |
| 五场景经 `VerificationService→SMS Dispatcher→数据库绑定` 独立选模并转为 accepted | 通过（Mock 供应商，不等于真实入口收件） |
| 首次 429 与幂等重放的 `Retry-After`/响应体精确一致 | 通过；恢复秒数仅内部持久化，不对日志列表公开 |
| SMS/auth/bootstrap 定向回归 | 通过 |
| `go test ./... -count=1` | 通过 |
| `go vet ./...` | 通过，无输出 |
| `go mod verify` | 通过，`all modules verified` |
| `tests/email/sensitive_scan_selftest.py` | 通过，4/4 |
| 阶段 2 范围敏感信息扫描 | 通过，FAIL=0、REVIEW=0 |
| `git diff --check` | 通过 |
| 换绑手机号/邮箱后清除旧管理员 MFA 回归 | 通过；认证模块及全仓回归通过，待 PR 最新 CI 和重新部署复证 |
| MySQL 8 `000001→000059→000058→000059` 测试资产 | 本机安全跳过；GitHub Actions 运行 #373 的 MySQL 8 门禁通过 |
| Linux `go test -v -race -count=1 ./...` | GitHub Actions 运行 #373 通过 |

## 未执行和残余门禁

1. 本机没有 Docker、MySQL 客户端和可用的隔离 MySQL 8；`SMS_MIGRATION_TEST_DSN` 未配置，因此本机迁移测试按显式门禁安全跳过。远程 MySQL 8 `1→59→58→59` 隔离矩阵已由 GitHub Actions 运行 #373 补证。
2. 当前 `SMS_ENABLED=false`。模板同步窗口只执行只读供应商模板查询；真实发送窗口因管理员绑定号码不满足真实收件前提而未启动，未调用阿里云 SendSms、未产生本轮短信费用。
3. Redis Lua 的键、原子计数、TTL、重试秒数与失败关闭已通过单测；真实 Redis 7 原子计数、限流和 TTL 已由 GitHub Actions 运行 #373 补证，恢复周期仍需独立 QA 复核。
4. 尚未由独立测试工程师复核，也未由产品经理正式批准。
5. PR #315 已创建，预合并修复提交的三项 CI 已通过，尚未完成独立正式评审和合并。
6. 当前 Windows Go 环境未启用 CGO，`go test -race` 无法执行；Linux race 已由 GitHub Actions 运行 #373 补证。

真实验证需单独授权，并限定白名单号码、单次窗口、费用上限和回滚动作；阿里云返回 `OK/accepted` 后仍需人工确认实际收件。

## 提交前完成度审查修复

1. 修复限流首次响应记录未保存恢复秒数、导致幂等重放退回默认 60 秒的问题；仓储、服务和 HTTP 层均补充回归测试。
2. 修复 `000059` down 错误引用 `group_permissions.permission_id` 的问题；实际表以 `id` 标识引用行，静态契约新增禁止旧错误列名的断言。
3. 补齐场景绑定和模板启停在服务层冲突、模板失效等失败结果下的审计记录，并验证摘要不包含号码或幂等键。
4. 修复 `000059` 新增 `submitted_at` 时把阶段 1 历史发送记录改写为迁移执行时间的问题；迁移现在用原 `created_at` 回填，并增加真实 MySQL 历史数据回归断言。
5. 修复模板列表逐条查询绑定场景形成的 N+1 查询；当前页模板改为一次批量查询场景绑定，并通过 SQL Mock 约束查询次数。
6. 修复供应商实现忽略上下文取消时模板同步可能超过 10 秒接口期限的问题；服务层现在以硬截止时间返回且超时零写入。
7. 修复固定短信签名为空时仍可启用模板的问题；启用操作现在失败关闭，并统一映射为 `503/50300`，避免错误暴露为内部 `500`。
8. 修复用户换绑或管理员代改手机号/邮箱后仍保留旧管理员 MFA 时间戳的问题；联系方式、基础 verified 与对应 `admin_*_verified_at=NULL` 现在原子更新，并增加“仅修改状态不得误清 MFA”的回归测试。

以上问题均已在当前工作树关闭，当前未发现仍未解决的 P0/P1；换绑 MFA 修复推送后仍须由 PR 最新 CI 重新证明 MySQL 8、Redis 7 和 Linux race 全部通过，并在测试服重新部署后才允许修改管理员 259 的绑定手机号。
