# 阿里云短信验证码阶段 2 测试报告

## 结论

当前远端代码基线为 `79ac4d0`，PR #315 对应的 GitHub 三项检查全部通过且无合并冲突；本地全仓测试、vet 和依赖校验通过。隔离测试服已部署该提交对应的 Linux 二进制，远端 SHA-256 为 `2e869f7ad9a4f93ac1cb252ed1a03de4fc20d05954c4bea1c5af0b6bca1c6b14`，数据库部署前后均为 `59:0`，本次未执行 migration。

2026-08-04 已关闭历史“五场景共用单模板”P1：阿里云只读查询确认 5 个目标模板使用同一签名、不同模板编码，均为验证码类型、审核通过且变量集合精确为 `code`。管理 API 同步首次结果为新增 4、未变化 1；重复同步为新增 0、更新 0、未变化 5。五个模板均已本地启用，`register/login/reset_password/bind_phone/admin_verify` 分别绑定模板编码后四位 `5137/0141/0117/0118/5093`，数据库中 5 个有效场景对应 5 个不同 `template_id`。

在单独授权的真实窗口内完成 1 次管理测试发送和五业务发码入口，共新增 6 条 `accepted`/`Code=OK` 记录，失败 0；用途分布为 `test=1`、`otp=5`，五条业务验证码记录均为 `accepted`。用户确认实际收到 6 条短信，并确认统一签名“爱斯琴山东网络科技”及注册、登录、管理员验证、找回密码、换绑手机场景文案均正确。该人工确认是实际收件证据，`accepted` 仍只表述为供应商受理。

窗口结束后经独立检查确认健康 200、单 API 进程、`SMS_ENABLED=false`、`SMS_TEST_MODE=true`、管理员认证有效期 24 小时及原白名单 1 个号码。验证编排曾因空占位环境键和退出进程短暂 zombie 状态产生两次 0 发送失败及一次恢复结果误判；最终发送预算为 6/10，业务服务和数据库证据完整，临时脚本已修复且未重发。`79ac4d0` 的九管理 API 部署态复核已通过且未产生短信；独立 QA、产品经理和正式代码评审均已通过，三条公开业务验证码消费 E2E 转入阶段 4 待办。六份最终验收文档已提交，PR #315 已于 2026-08-04 Squash 合并至 `main@9e50ee1`，阶段 2 正式闭环。

## 已执行

| 检查 | 结果 |
|---|---|
| 阿里云模板只读同步与重复同步 | 通过；首次新增 4、未变化 1；重复同步 0 新增、0 更新、5 未变化 |
| 固定五场景各自唯一有效绑定的技术约束 | 通过；每个场景只有一个有效绑定 |
| 五场景分别使用独立模板和正确场景文案 | 通过；5 个不同模板、同一签名，用户确认 6 条实际收件的签名与场景文案均正确 |
| 九管理接口权限、MFA、参数和 D-95 分页 | 通过自动化和 `79ac4d0` 隔离部署态 HTTP；管理员 259 Token/MFA 通过，读取接口 200/0，冲突写入 409，关闭态测试发送 503 |
| 幂等重放、参数冲突、16 路并发单外呼 | 通过 |
| 白名单、管理员/手机号双维 Redis Lua 限流和失败关闭 | 单元测试与 Redis 7 CI 通过 |
| MySQL 8 `000001→000059→000058→000059` | `79ac4d0` 对应 GitHub 检查通过；本次部署未执行 migration，schema 保持 `59:0` |
| Linux `go test -v -race -count=1 ./...` | `79ac4d0` 对应 GitHub 检查通过 |
| `go test ./... -count=1`、`go vet ./...`、`go mod verify` | 通过 |
| 本地阶段范围敏感扫描 | 通过，FAIL=0、REVIEW=0 |
| 隔离测试环境运行时敏感扫描 | 通过；数据库/审计/API 日志各敏感项计数均为 0 |
| 换绑手机号/邮箱后清除旧管理员 MFA | 通过；本地回归、CI、重新部署与真实换绑行为复证 |
| 管理测试发送 | 使用 `register` 独立模板；阿里云受理并由用户确认实际收件 |
| `register` | 使用后四位 `5137` 模板；阿里云受理并由用户确认实际收件；注册与验证码消费 E2E 转入阶段 4 |
| `login` | 使用后四位 `0141` 模板；阿里云受理并由用户确认实际收件；登录与验证码消费 E2E 转入阶段 4 |
| `reset_password` | 使用后四位 `0117` 模板；阿里云受理并由用户确认实际收件；重置密码与验证码消费 E2E 转入阶段 4 |
| `bind_phone` | 使用后四位 `0118` 模板；阿里云受理并由用户确认实际收件 |
| `admin_verify` | 使用后四位 `5093` 模板；阿里云受理并由用户确认实际收件 |

## 本轮五模板真实验证计数与口径

- 本轮新增 `sms_send_logs=6`，`accepted=6`、`failed=0`、`provider_code=OK=6`。
- 用途分布：`test=1`、`otp=5`；场景分布为 `register=2`，其余四个场景各 1。
- 五条业务验证码记录为连续最新记录，场景覆盖 `login/register/admin_verify/reset_password/bind_phone`，`send_status` 均为 `accepted`。
- 用户确认收到 6 条，并确认签名及六条场景文案正确；报告不记录验证码或完整手机号。
- 6 次受理可能产生计费，低于本轮 10 条和总预算 500 条；实际账单金额未核对。
- 历史 7 条受理记录继续保留作为前次验证证据，但不与本轮六项证据混算。

## 未执行和残余门禁

1. **阶段 4 待办：** `register/login/reset_password` 已完成真实发码和收件；使用真实验证码继续完成注册、登录、重置密码业务消费 E2E 尚未执行，但不属于阶段 2 P2。
2. `79ac4d0` 已完成 GitHub 三项检查、隔离部署和九管理 API HTTP；重复同步新增 0、更新 0、未变化 5，冲突写接口均为 409，关闭态测试发送为 503，发送日志前后均为 13。
3. 独立 QA 最终签署阶段 2 通过，限定 `79ac4d0`，P0=0、P1=0、P2=0、P3=0。
4. 产品经理批准阶段 2 产品验收；PR #315 已合并，允许进入阶段 3。
5. PR #315 正式代码评审和三项 CI 均通过，已以 `9e50ee1` 合并至 `main`。
6. 15 条已推送历史提交的中文说明缺少仓库要求的“模块：描述”前缀；不得未经授权 force push。若使用一条合规中文 squash commit 可避免旧提交进入 `main`，但这又需要项目负责人明确批准偏离默认 Merge commit 流程。
7. 测试服原配置仍保留三个值为空的废弃短信键名；真实窗口临时移除，窗口结束后按授权恢复。未来再次开启前应单独批准并永久清理。
8. 当前仅验证小流量白名单链路，不代表全量运营商送达率或长期稳定性。

## 已关闭的 P1

真实验证准备期间发现“换绑手机号或邮箱后仍继承旧管理员 MFA”的代码 P1，提交 `34b69a4` 已修复并通过真实换绑/重新验证复证。独立产品复核随后发现短信管理写接口忽略审计失败，提交 `7352931` 已实现请求审计前置失败关闭；预合并评审又发现额外变量模板可能放行、结果审计失败会覆盖真实业务结果，提交 `d440e29` 已增加四层精确变量门禁，并保持请求审计失败关闭、结果审计失败安全告警且不误报。历史“五场景共用单模板”外部配置 P1 已于 2026-08-04 通过五个同签名独立模板、管理 API 绑定和 6 条人工收件文案确认关闭；当前开发侧未发现未关闭 P0/P1，仍以修复后独立复验结论为准。
