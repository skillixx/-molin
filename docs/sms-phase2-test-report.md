# 阿里云短信验证码阶段 2 本地测试报告

## 结论

截至当前阶段 2 工作树，后端开发自测通过，状态为“代码完成，等待 MySQL 8 迁移验证、测试工程师验收、产品经理确认及受控真实验证”，不能表述为生产可用。

## 已执行

| 检查 | 结果 |
|---|---|
| 阿里云模板列表 + 新版详情适配器单测 | 通过 |
| 同步失败零写入、固定签名/验证码模板过滤 | 通过 |
| 模板概览、启停 CAS、场景固定集合与九路由权限 | 通过 |
| 测试发送关闭态、首次提交、accepted 幂等重放 | 通过 |
| SMS/auth/bootstrap 定向回归 | 通过 |
| `go test ./... -count=1` | 通过 |
| `go vet ./...` | 通过，无输出 |
| `git diff --check` | 通过 |

## 未执行和残余门禁

1. 本机没有 Docker 与 MySQL 客户端，尚未执行 MySQL 8 的 `1→59` 和 `58→59→58→59` 隔离矩阵。
2. `SMS_ENABLED` 保持关闭，未使用真实密钥、未调用阿里云 SendSms、未产生短信费用。
3. 未执行真实 Redis 的双维限流恢复周期测试。
4. 尚未由独立测试工程师复核，也未由产品经理正式批准。
5. 尚未推送分支、创建 PR、运行远程 CI 或合并。
6. 当前 Windows Go 环境未启用 CGO，`go test -race` 无法执行；并发正确性已由数据库唯一约束、乐观锁及单元测试覆盖，race 仍应交由 Linux CI 补跑。

真实验证需单独授权，并限定白名单号码、单次窗口、费用上限和回滚动作；阿里云返回 `OK/accepted` 后仍需人工确认实际收件。
