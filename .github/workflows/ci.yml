name: CI

on:
  pull_request:
    branches: [main]

jobs:
  change-scope:
    name: CI 变更范围分类
    runs-on: ubuntu-latest
    outputs:
      docs_lightweight: ${{ steps.scope.outputs.docs_lightweight }}
      backend: ${{ steps.scope.outputs.backend }}
      phase5: ${{ steps.scope.outputs.phase5 }}
      gateway_g3: ${{ steps.scope.outputs.gateway_g3 }}
      gateway_g4: ${{ steps.scope.outputs.gateway_g4 }}
      gateway_g7: ${{ steps.scope.outputs.gateway_g7 }}
      gateway_g8: ${{ steps.scope.outputs.gateway_g8 }}
      gateway_g8_real_e2e: ${{ steps.scope.outputs.gateway_g8_real_e2e }}
      frontend_admin: ${{ steps.scope.outputs.frontend_admin }}
      frontend_user: ${{ steps.scope.outputs.frontend_user }}
      full: ${{ steps.scope.outputs.full }}
    steps:
      - uses: actions/checkout@v4
        with:
          # 精确比较 PR 基线与头提交，不能依赖浅克隆猜测变更范围。
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: 校验并执行 CI 变更范围分类
        id: scope
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          python -m unittest infra/scripts/test_classify_ci_change_scope.py
          python -m py_compile infra/scripts/classify-ci-change-scope.py infra/scripts/test_classify_ci_change_scope.py
          python infra/scripts/classify-ci-change-scope.py \
            --base-sha="$BASE_SHA" \
            --head-sha="$HEAD_SHA" \
            --repo-root=. \
            --github-output="$GITHUB_OUTPUT"

  docs-lightweight:
    name: 文档与基础质量轻量门禁
    needs: change-scope
    if: needs.change-scope.outputs.docs_lightweight == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: 校验差异格式
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: git diff --check "$BASE_SHA...$HEAD_SHA"

      - name: 扫描差异敏感信息
        run: python scripts/verify-sms-phase5-sensitive-data.py --repo-root . --base-ref "${{ github.event.pull_request.base.sha }}"

  # ---- 后端 CI ----
  backend:
    name: 后端构建与测试
    needs: change-scope
    if: needs.change-scope.outputs.backend == 'true'
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root_password
          MYSQL_DATABASE: molin_sms_test_ci
          MYSQL_USER: molin
          MYSQL_PASSWORD: molin_password
        ports: ["3306:3306"]
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3
      redis:
        image: redis:7
        ports: ["6379:6379"]
        options: --health-cmd="redis-cli ping" --health-interval=10s --health-retries=3

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: 安装 Go
        uses: actions/setup-go@v5
        with:
          go-version-file: server/go.mod
          cache-dependency-path: server/go.sum

      - name: 安装依赖
        working-directory: server
        run: go mod download

      - name: 校验模块依赖
        working-directory: server
        run: go mod verify

      - name: 校验 GitHub Actions 工作流
        run: go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.7 .github/workflows/*.yml

      - name: 代码检查（go vet）
        working-directory: server
        run: go vet ./...

      - name: 运行单元测试
        working-directory: server
        env:
          APP_ENV: test
          MYSQL_HOST: 127.0.0.1
          MYSQL_PORT: 3306
          MYSQL_DATABASE: molin_sms_test_ci
          MYSQL_USER: molin
          MYSQL_PASSWORD: molin_password
          REDIS_ADDR: 127.0.0.1:6379
          JWT_SECRET: test_jwt_secret_for_ci_only_32chars
          REFRESH_TOKEN_SECRET: test_refresh_secret_for_ci_32chars
          ID_CARD_HMAC_SECRET: test_hmac_secret_for_ci_only_32c
          SMS_MIGRATION_TEST_DSN: "molin:molin_password@tcp(127.0.0.1:3306)/molin_sms_test_ci?parseTime=true&multiStatements=true"
          SMS_REDIS_INTEGRATION_TEST: "1"
        run: go test -v -race -count=1 ./...

      - name: 构建确认
        working-directory: server
        run: go build -o /dev/null ./cmd/api

      - name: 隔离验证 G6 MySQL 8 迁移
        env:
          AI_GATEWAY_G6_ISOLATED_APPROVED: "YES"
          G6_DOCKER_PULL_POLICY: missing
        run: bash infra/scripts/verify-ai-gateway-g6-customer.sh

  phase5-release-safety:
    name: 阶段 5 短信发布安全门禁
    needs: change-scope
    if: needs.change-scope.outputs.phase5 == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # 敏感信息门禁需要比较阶段分支与主分支的完整共同基线。
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: 安装 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: |
            web/admin-console/package-lock.json
            web/user-console/package-lock.json

      - name: 构建管理端扫描产物
        working-directory: web/admin-console
        run: |
          npm ci
          npm run build

      - name: 构建用户端扫描产物
        working-directory: web/user-console
        run: |
          npm ci
          npm run build

      - name: 校验 Nginx 来源头安全边界
        run: python infra/nginx/verify_forwarded_headers.py

      - name: 校验短信发布准备度
        shell: pwsh
        run: ./scripts/verify-sms-phase5-readiness.ps1 -SelfTest -RunSensitiveScan

      - name: 校验阶段 5 敏感信息门禁契约
        run: python tests/sms/phase5_sensitive_data_gate_contract.py

      - name: 校验代理专用网络规划
        shell: pwsh
        run: ./scripts/verify-sms-phase5-proxy-network-plan.ps1 -SelfTest

      - name: 校验测试服回滚材料与告警通知只读预检资产
        run: |
          python tests/sms/phase5_recovery_notification_preflight_contract.py
          bash -n scripts/verify-sms-phase5-test-server-recovery-readiness.sh

      - name: 校验阶段 5 固定测试服 SSH 身份共享契约
        run: python tests/sms/phase5_ssh_identity_contract.py

      - name: 校验测试服关闭态只读审计资产
        run: |
          python tests/sms/phase5_test_server_readonly_contract.py
          bash -n scripts/verify-sms-phase5-test-server-readonly.sh

      - name: 离线校验测试服关闭态只读审计包装器
        shell: pwsh
        run: ./scripts/verify-sms-phase5-test-server-readonly.ps1 -SelfTest

      - name: 校验测试服前端部署与自动回滚契约
        run: python tests/sms/phase5_deploy_workflow_contract.py

      - name: 离线校验测试服回滚与通知预检包装器
        shell: pwsh
        run: ./scripts/verify-sms-phase5-test-server-recovery-readiness.ps1 -SelfTest

      - name: 校验测试服回滚候选生成资产
        run: |
          python tests/sms/phase5_rollback_candidate_contract.py
          bash -n scripts/prepare-sms-phase5-test-server-rollback-candidate.sh

      - name: 离线校验测试服回滚候选包装器
        shell: pwsh
        run: ./scripts/prepare-sms-phase5-test-server-rollback-candidate.ps1 -SelfTest

      - name: 校验测试服回滚候选只读验证资产
        run: |
          python tests/sms/phase5_rollback_candidate_verifier_contract.py
          bash -n scripts/verify-sms-phase5-test-server-rollback-candidate.sh
          bash scripts/verify-sms-phase5-test-server-rollback-candidate.sh --self-test

      - name: 离线校验测试服回滚候选只读包装器
        shell: pwsh
        run: ./scripts/verify-sms-phase5-test-server-rollback-candidate.ps1 -SelfTest

      - name: 校验测试服旧二进制回滚与当前版本恢复演练资产
        run: |
          python tests/sms/phase5_rollback_runtime_drill_contract.py
          bash -n scripts/run-sms-phase5-test-server-rollback-drill.sh

      - name: 离线校验测试服回滚恢复演练包装器
        shell: pwsh
        run: ./scripts/prepare-sms-phase5-test-server-rollback-drill.ps1 -ChangeId 20990101T000000Z -SelfTest

      - name: 校验测试服回滚候选暂存与执行后独立验收资产
        run: |
          python tests/sms/phase5_rollback_runtime_evidence_contract.py
          bash -n scripts/stage-sms-phase5-test-server-rollback-drill.sh
          bash -n scripts/verify-sms-phase5-test-server-rollback-drill.sh

      - name: 校验回滚实际执行包装器安全契约
        run: python tests/sms/phase5_rollback_runtime_execution_contract.py

      - name: 离线校验测试服回滚候选暂存包装器
        shell: pwsh
        run: ./scripts/stage-sms-phase5-test-server-rollback-drill.ps1 -SelfTest

      - name: 离线校验测试服回滚执行证据验证器
        shell: pwsh
        run: ./scripts/verify-sms-phase5-test-server-rollback-drill.ps1 -SelfTest

      - name: 校验测试服日志留存只读审计资产
        run: |
          python tests/sms/phase5_log_retention_preflight_contract.py
          bash -n scripts/verify-sms-phase5-test-server-log-retention.sh

      - name: 离线校验测试服日志留存审计包装器
        shell: pwsh
        run: ./scripts/verify-sms-phase5-test-server-log-retention.ps1 -SelfTest

      - name: 校验 Alertmanager 通知演练只读预检资产
        run: |
          python tests/sms/phase5_alertmanager_drill_readiness_contract.py
          bash -n scripts/verify-sms-phase5-alertmanager-drill-readiness.sh

      - name: 离线校验 Alertmanager 通知演练只读预检包装器
        shell: pwsh
        run: ./scripts/verify-sms-phase5-alertmanager-drill-readiness.ps1 -SelfTest

      - name: 校验测试服 Canary 只读聚合预检
        run: python tests/sms/phase5_canary_preflight_contract.py

      - name: 校验 Canary 执行计划与五档观察证据门禁
        run: |
          python tests/sms/phase5_canary_execution_plan_contract.py
          python tests/sms/phase5_canary_plan_candidate_contract.py
          python tests/sms/phase5_canary_target_intake_candidate_contract.py
          python tests/sms/phase5_canary_target_state_readonly_candidate_contract.py
          python tests/sms/phase5_canary_whitelist_change_candidate_contract.py
          python tests/sms/phase5_canary_send_candidate_contract.py
          python tests/sms/phase5_canary_failure_readonly_diagnostic_contract.py
          python tests/sms/phase5_canary_postcheck_readonly_candidate_contract.py
          python tests/sms/phase5_enabled_startup_readonly_diagnostic_contract.py
          python tests/sms/phase5_legacy_config_cleanup_candidate_contract.py
          python tests/sms/phase5_observation_evidence_contract.py
          python tests/sms/phase5_observation_progress_contract.py
          python tests/sms/phase5_observation_assembler_contract.py
          python tests/sms/phase5_final_state_assembler_contract.py
          python tests/sms/phase5_observation_snapshot_candidate_contract.py
          python tests/sms/phase5_production_target_intake_candidate_contract.py
          python tests/sms/phase5_production_readonly_baseline_candidate_contract.py
          python tests/sms/phase5_production_closed_deployment_plan_contract.py

      - name: 离线校验测试服 Canary 聚合预检包装器
        shell: pwsh
        run: ./scripts/verify-sms-phase5-test-server-canary-preflight.ps1 -SelfTest

      - name: 校验测试服日志留存变更资产
        run: |
          python tests/sms/phase5_log_retention_change_contract.py
          bash -n scripts/apply-sms-phase5-test-server-log-retention.sh
          bash scripts/apply-sms-phase5-test-server-log-retention.sh --self-test

      - name: 离线校验测试服日志留存变更包装器
        shell: pwsh
        run: ./scripts/apply-sms-phase5-test-server-log-retention.ps1 -SelfTest

      # 测试服只读审计依赖专用 SSH 凭据，不在不可信 PR 上自动连接外部环境；部署窗口由运维手工执行对应脚本。

      - name: 使用 promtool 校验告警规则
        run: |
          docker run --rm --entrypoint=/bin/promtool \
            --workdir=/rules \
            -v "${{ github.workspace }}/infra/prometheus:/rules:ro" \
            prom/prometheus:v3.12.0-distroless \
            check rules email-alerts.yml
          docker run --rm --entrypoint=/bin/promtool \
            --workdir=/rules \
            -v "${{ github.workspace }}/infra/prometheus:/rules:ro" \
            prom/prometheus:v3.12.0-distroless \
            test rules email-alerts.test.yml

      - name: 演练测试服与生产关闭回滚
        shell: pwsh
        run: |
          ./scripts/verify-sms-phase5-rollback-dry-run.ps1 -Environment test -CurrentSmsEnabled false
          ./scripts/verify-sms-phase5-rollback-dry-run.ps1 -Environment production -CurrentSmsEnabled true

  gateway-g3:
    name: AI 网关 G3 财务与消息验收
    needs: change-scope
    if: needs.change-scope.outputs.gateway_g3 == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: 隔离验证 MySQL 8 与 RabbitMQ 恢复
        env:
          AI_GATEWAY_G3_MYSQL_APPROVED: "YES"
          G3_DOCKER_PULL_POLICY: missing
        run: bash infra/scripts/verify-ai-gateway-migration-000062.sh

  gateway-g4:
    name: AI 网关 G4 安全与资源治理验收
    needs: change-scope
    if: needs.change-scope.outputs.gateway_g4 == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: 隔离验证 MySQL 8、Redis 7 与 RabbitMQ 恢复
        env:
          AI_GATEWAY_G4_ISOLATED_APPROVED: "YES"
          G4_DOCKER_PULL_POLICY: missing
        run: bash infra/scripts/verify-ai-gateway-g4-governance.sh

  gateway-g7:
    name: AI 网关 G7 可靠性与零差额验收
    needs: change-scope
    if: needs.change-scope.outputs.gateway_g7 == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # G7 敏感信息扫描需要完整比较当前 PR 与 main 的共同基线。
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: 校验 G7 变更 Go 文件格式
        run: |
          mapfile -t changed_go_files < <(git diff --name-only --diff-filter=ACMR origin/main...HEAD -- ':(glob)server/**/*.go')
          if ((${#changed_go_files[@]} > 0)); then
            unformatted="$(gofmt -l "${changed_go_files[@]}")"
            test -z "$unformatted" || { printf '以下 G7 Go 文件未通过 gofmt：\n%s\n' "$unformatted"; exit 1; }
          fi

      - name: 校验 G7 脚本和可观测资产
        env:
          INTERNAL_API_TOKEN: test_internal_token_for_g7_ci_only_32chars
        run: |
          bash -n infra/scripts/verify-ai-gateway-g7-reliability.sh
          python -m json.tool infra/grafana/dashboards/ai-gateway-slo.json >/dev/null
          docker compose --profile monitoring -f infra/docker-compose.yml config --quiet
          test "$(grep -c '^[[:space:]]*- alert:' infra/prometheus/ai-gateway-alerts.yml)" -eq "$(grep -c '^[[:space:]]*runbook_url:' infra/prometheus/ai-gateway-alerts.yml)"
          test "$(grep -c '^[[:space:]]*- alert:' infra/prometheus/ai-gateway-alerts.yml)" -eq "$(grep -c '^[[:space:]]*p_level:' infra/prometheus/ai-gateway-alerts.yml)"
          while IFS= read -r anchor; do
            grep -Fq "<a id=\"${anchor}\"></a>" docs/ai-gateway-g7-alert-runbook.md
          done < <(sed -n 's/.*runbook_url:.*#\([^[:space:]]*\).*/\1/p' infra/prometheus/ai-gateway-alerts.yml)
          docker run --rm --entrypoint=/bin/promtool \
            --workdir=/rules \
            -v "${{ github.workspace }}/infra/prometheus:/rules:ro" \
            prom/prometheus:v3.12.0-distroless \
            check rules ai-gateway-alerts.yml
          docker run --rm --entrypoint=/bin/promtool \
            --workdir=/rules \
            -v "${{ github.workspace }}/infra/prometheus:/rules:ro" \
            prom/prometheus:v3.12.0-distroless \
            test rules ai-gateway-alerts.test.yml

      - name: 扫描 G7 分支敏感信息
        run: python scripts/verify-sms-phase5-sensitive-data.py --repo-root . --base-ref origin/main

      - name: 隔离验证 100 并发、幂等、断连、混沌与只读对账
        env:
          AI_GATEWAY_G7_ISOLATED_APPROVED: "YES"
          G7_DOCKER_PULL_POLICY: missing
        run: bash infra/scripts/verify-ai-gateway-g7-reliability.sh

  gateway-g8:
    name: AI 网关 G8 生产就绪门禁
    needs: change-scope
    if: needs.change-scope.outputs.gateway_g8 == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          # 冻结对账器由该精确工具链构建；GOTOOLCHAIN=local 禁止生成器静默切换版本。
          go-version: 1.26.5
          cache-dependency-path: server/go.sum

      - name: 校验生产配置、流量总闸和渠道健康安全策略
        working-directory: server
        run: |
          go test ./internal/config ./internal/modules/token_gateway/service ./internal/modules/token_gateway/handler ./internal/modules/token_gateway ./internal/bootstrap
          go vet ./internal/config ./internal/modules/token_gateway/... ./internal/bootstrap

      - name: 校验 G8 测试服到生产迁移清单
        run: |
          python -m unittest infra/scripts/test_verify_ai_gateway_g8_migration_manifest.py
          python -m unittest infra/scripts/test_audit_ai_gateway_g8_test_server_readonly.py
          python -I -m unittest discover -s infra/scripts -p test_prepare_ai_gateway_g8_test_readonly_access_bundle.py
          python -I -m unittest discover -s infra/scripts -p test_run_ai_gateway_g8_test_readonly_access_stage.py
          python -I -m unittest discover -s infra/scripts -p test_run_ai_gateway_g8_test_staging_evidence.py
          python -I -m unittest discover -s infra/scripts -p test_run_ai_gateway_g8_test_readonly_transport_diagnostic.py
          python -I -m py_compile infra/scripts/prepare-ai-gateway-g8-test-readonly-access-bundle.py infra/scripts/test_prepare_ai_gateway_g8_test_readonly_access_bundle.py infra/scripts/run-ai-gateway-g8-test-readonly-access-stage.py infra/scripts/test_run_ai_gateway_g8_test_readonly_access_stage.py infra/scripts/run-ai-gateway-g8-test-staging-evidence.py infra/scripts/test_run_ai_gateway_g8_test_staging_evidence.py infra/scripts/run-ai-gateway-g8-test-readonly-transport-diagnostic.py infra/scripts/test_run_ai_gateway_g8_test_readonly_transport_diagnostic.py
          python -I infra/scripts/prepare-ai-gateway-g8-test-readonly-access-bundle.py --self-test
          python -I infra/scripts/run-ai-gateway-g8-test-readonly-access-stage.py --self-test
          python -I infra/scripts/run-ai-gateway-g8-test-staging-evidence.py --self-test
          python -I infra/scripts/run-ai-gateway-g8-test-readonly-transport-diagnostic.py --self-test
          bundle_dir="${RUNNER_TEMP}/g8-test-readonly-access-bundle-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          test ! -e "$bundle_dir"
          set +e
          python -I infra/scripts/prepare-ai-gateway-g8-test-readonly-access-bundle.py \
            --change-id=CHG-G8-TEST-READONLY-ACCESS-20260812-003 \
            --source-commit=8ec878572f62ef2584c38aaadc1bca1cb802b13f \
            --output-dir="$bundle_dir" >/tmp/g8-test-readonly-access-consumed-replay.log 2>&1
          status=$?
          set -e
          test "$status" -eq 2
          test ! -e "$bundle_dir"
          grep -qx 'G8_TEST_READONLY_ACCESS_BUNDLE=FAILED reason=invalid_request' /tmp/g8-test-readonly-access-consumed-replay.log
          python -I infra/scripts/prepare-ai-gateway-g8-test-readonly-access-bundle.py \
            --verify-consumed-candidate \
            --consumed-change-id=CHG-G8-TEST-READONLY-ACCESS-20260812-001 | tee /tmp/g8-test-readonly-access-consumed-001.log
          grep -qx 'G8_TEST_READONLY_ACCESS_BUNDLE_VERIFY=PASS' /tmp/g8-test-readonly-access-consumed-001.log
          grep -qx 'change_id=CHG-G8-TEST-READONLY-ACCESS-20260812-001' /tmp/g8-test-readonly-access-consumed-001.log
          grep -qx 'source_commit=c50f092339fcad79ca1262925480219db1755318' /tmp/g8-test-readonly-access-consumed-001.log
          grep -qx 'source_tree=2e9701c3f5d8ba12aebc9631b01696b189f1d313' /tmp/g8-test-readonly-access-consumed-001.log
          grep -qx 'auditor_sha256=308908d2a2b9fa8679fd21d77fde68b5ce5d521ed37dac6b7726e6c323452256' /tmp/g8-test-readonly-access-consumed-001.log
          grep -qx 'sudoers_sha256=1ec266c71f00d99da18b9e8cf59af91d6126811384adef62ce48750b97a0986f' /tmp/g8-test-readonly-access-consumed-001.log
          grep -qx 'reconcile_sha256=37f6ee369f1ce489a3966123dfea3bd172d5386045495e069433c7f3d993f2c1' /tmp/g8-test-readonly-access-consumed-001.log
          grep -qx 'reconcile_size=13066129' /tmp/g8-test-readonly-access-consumed-001.log
          grep -qx 'bundle_receipt_sha256=14b7d8cd832f0b719031fcc93adbbb2208afe76d34383e63d51c44b044772b5a' /tmp/g8-test-readonly-access-consumed-001.log
          python -I infra/scripts/prepare-ai-gateway-g8-test-readonly-access-bundle.py \
            --verify-consumed-candidate \
            --consumed-change-id=CHG-G8-TEST-READONLY-ACCESS-20260812-002 | tee /tmp/g8-test-readonly-access-consumed-002.log
          grep -qx 'G8_TEST_READONLY_ACCESS_BUNDLE_VERIFY=PASS' /tmp/g8-test-readonly-access-consumed-002.log
          grep -qx 'change_id=CHG-G8-TEST-READONLY-ACCESS-20260812-002' /tmp/g8-test-readonly-access-consumed-002.log
          grep -qx 'source_commit=50b3e2f9d18b38e7d4a91ebeb4f03c413ef33c44' /tmp/g8-test-readonly-access-consumed-002.log
          grep -qx 'source_tree=73fb652a1f86db84991c8745f8c10e1d2a255f29' /tmp/g8-test-readonly-access-consumed-002.log
          grep -qx 'auditor_sha256=308908d2a2b9fa8679fd21d77fde68b5ce5d521ed37dac6b7726e6c323452256' /tmp/g8-test-readonly-access-consumed-002.log
          grep -qx 'sudoers_sha256=1ec266c71f00d99da18b9e8cf59af91d6126811384adef62ce48750b97a0986f' /tmp/g8-test-readonly-access-consumed-002.log
          grep -qx 'reconcile_sha256=37f6ee369f1ce489a3966123dfea3bd172d5386045495e069433c7f3d993f2c1' /tmp/g8-test-readonly-access-consumed-002.log
          grep -qx 'reconcile_size=13066129' /tmp/g8-test-readonly-access-consumed-002.log
          grep -qx 'bundle_receipt_sha256=7ae580cc06fb101fe44c9e3a4d7581116fd258ef1e2d09d99bba0bda50151a1f' /tmp/g8-test-readonly-access-consumed-002.log
          BASH_ENV=/tmp/nonexistent-bash-env \
            GIT_DIR=/tmp/nonexistent-git-dir \
            GIT_CONFIG_COUNT=1 \
            GIT_CONFIG_KEY_0=core.autocrlf \
            GIT_CONFIG_VALUE_0=true \
            GOPROXY=https://invalid.example \
            TAR_OPTIONS='--checkpoint=1 --checkpoint-action=exec=echo BAD_ENV_INJECTION' \
            python -I infra/scripts/prepare-ai-gateway-g8-test-readonly-access-bundle.py \
            --verify-consumed-candidate \
            --consumed-change-id=CHG-G8-TEST-READONLY-ACCESS-20260812-003 | tee /tmp/g8-test-readonly-access-consumed-003.log
          if grep -q 'BAD_ENV_INJECTION' /tmp/g8-test-readonly-access-consumed-003.log; then
            exit 1
          fi
          grep -qx 'G8_TEST_READONLY_ACCESS_BUNDLE_VERIFY=PASS' /tmp/g8-test-readonly-access-consumed-003.log
          grep -qx 'change_id=CHG-G8-TEST-READONLY-ACCESS-20260812-003' /tmp/g8-test-readonly-access-consumed-003.log
          grep -qx 'source_commit=8ec878572f62ef2584c38aaadc1bca1cb802b13f' /tmp/g8-test-readonly-access-consumed-003.log
          grep -qx 'source_tree=988bdcdc8017322264733ebe68876e4811b01412' /tmp/g8-test-readonly-access-consumed-003.log
          grep -qx 'auditor_sha256=308908d2a2b9fa8679fd21d77fde68b5ce5d521ed37dac6b7726e6c323452256' /tmp/g8-test-readonly-access-consumed-003.log
          grep -qx 'sudoers_sha256=1ec266c71f00d99da18b9e8cf59af91d6126811384adef62ce48750b97a0986f' /tmp/g8-test-readonly-access-consumed-003.log
          grep -qx 'reconcile_sha256=37f6ee369f1ce489a3966123dfea3bd172d5386045495e069433c7f3d993f2c1' /tmp/g8-test-readonly-access-consumed-003.log
          grep -qx 'reconcile_size=13066129' /tmp/g8-test-readonly-access-consumed-003.log
          # 003 的 Linux CI 历史回执与 Windows 本地安装候选回执不同；两者共享同一来源树、制品摘要和大小。
          grep -qx 'bundle_receipt_sha256=7f4633357bf6883d166b0ee7d9750d7e745cf0a15d23163a547d6519e217efc1' /tmp/g8-test-readonly-access-consumed-003.log
          command -v visudo >/dev/null
          sudo visudo -cf infra/sudoers/molin-g8-test-readonly-audit
          set +e
          python infra/scripts/verify-ai-gateway-g8-migration-manifest.py \
            --manifest infra/ai-gateway-g8-migration-manifest.example.json >/tmp/g8-manifest-example.log 2>&1
          status=$?
          set -e
          test "$status" -eq 2
          grep -q '^G8_MIGRATION_MANIFEST=FAILED' /tmp/g8-manifest-example.log

      - name: 校验生产监控、告警、Compose 和 Nginx 契约
        env:
          BIFROST_IMAGE_DIGEST: alpine:3.20
          AI_GATEWAY_ALERT_RELAY_IMAGE_DIGEST: alpine:3.20
          AI_GATEWAY_INTERNAL_TOKEN_FILE: /tmp/g8-internal-token
          AI_GATEWAY_ALERT_RELAY_CONFIG_FILE: /tmp/g8-alert-relay-config
          MOLIN_PROD_ENV_FILE: /tmp/g8-prod.env
          BIFROST_NODE_ENV_FILE: /tmp/g8-bifrost-node.env
          BIFROST_LB_ENV_FILE: /tmp/g8-bifrost-lb.env
        run: |
          touch /tmp/g8-internal-token /tmp/g8-alert-relay-config /tmp/g8-prod.env
          printf '%s\n' 'BIFROST_ENCRYPTION_KEY=ci-placeholder' 'OPENROUTER_API_KEY=ci-placeholder' 'BAILIAN_API_KEY=ci-placeholder' >/tmp/g8-bifrost-node.env
          printf '%s\n' 'BIFROST_INTERNAL_TOKEN=ci-placeholder' >/tmp/g8-bifrost-lb.env
          python infra/scripts/verify-ai-gateway-g8-secret-scope.py --node-env /tmp/g8-bifrost-node.env --lb-env /tmp/g8-bifrost-lb.env
          python infra/nginx/verify_forwarded_headers.py
          docker compose -f infra/docker-compose.prod.yml -f infra/docker-compose.ai-gateway-g8.yml config --format json >/tmp/g8-compose.json
          python - <<'PY'
          import json
          from pathlib import Path

          config = json.loads(Path('/tmp/g8-compose.json').read_text())
          services = config['services']
          assert 'ai-gateway-lb' in services['api']['networks']
          assert 'internal' not in services['bifrost-lb']['networks']
          public_networks = set(services['admin-console']['networks']) | set(services['user-console']['networks'])
          for name in ('bifrost-1', 'bifrost-2'):
              node_networks = set(services[name]['networks'])
              assert node_networks == {'ai-gateway-nodes', 'ai-gateway-egress'}, name
              assert not (node_networks & public_networks), name
              assert not (node_networks & set(services['api']['networks'])), name
          assert set(services['bifrost-lb']['networks']) == {'ai-gateway-lb', 'ai-gateway-nodes'}
          expected_node_keys = {'BIFROST_ENCRYPTION_KEY', 'OPENROUTER_API_KEY', 'BAILIAN_API_KEY'}
          assert set(services['bifrost-1'].get('environment', {})) == expected_node_keys
          assert set(services['bifrost-2'].get('environment', {})) == expected_node_keys
          assert set(services['bifrost-lb'].get('environment', {})) == {'BIFROST_INTERNAL_TOKEN'}
          PY
          for conf in admin.conf user.conf api.conf; do
            docker run --rm --add-host api:127.0.0.1 \
              -v "${{ github.workspace }}/infra/nginx/${conf}:/etc/nginx/conf.d/default.conf:ro" \
              -v "${{ github.workspace }}/infra/nginx/traffic-gate-closed:/etc/nginx/ai-gateway-traffic-gate:ro" \
              nginx:1.29.1-alpine nginx -t
          done
          docker run --rm --entrypoint=/bin/promtool \
            --workdir=/etc/prometheus \
            -v "${{ github.workspace }}/infra/prometheus/ai-gateway-production.yml:/etc/prometheus/ai-gateway-production.yml:ro" \
            -v "${{ github.workspace }}/infra/prometheus/ai-gateway-alerts.yml:/etc/prometheus/rules/ai-gateway-alerts.yml:ro" \
            -v "${{ github.workspace }}/infra/prometheus/targets:/etc/prometheus/targets:ro" \
            prom/prometheus:v3.12.0-distroless \
            check config ai-gateway-production.yml
          docker run --rm --entrypoint=/bin/amtool \
            --workdir=/config \
            -v "${{ github.workspace }}/infra/alertmanager:/config:ro" \
            prom/alertmanager:v0.28.1 \
            check-config ai-gateway-g8.yml
          bash infra/scripts/verify-ai-gateway-g8-alert-routing.sh

      - name: 执行 G8 生产形态部署与回滚演练
        env:
          AI_GATEWAY_G8_ISOLATED_APPROVED: YES
        run: |
          bash -n infra/scripts/verify-ai-gateway-g8-production-rehearsal.sh
          bash infra/scripts/verify-ai-gateway-g8-production-rehearsal.sh

      - name: 扫描 G8 差异敏感信息
        run: python scripts/verify-sms-phase5-sensitive-data.py --repo-root . --base-ref origin/main

  gateway-g8-real-e2e:
    name: AI 网关 G8 真实后端浏览器验收
    needs: change-scope
    if: needs.change-scope.outputs.gateway_g8_real_e2e == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: |
            web/admin-console/package-lock.json
            web/user-console/package-lock.json

      - name: 安装双端依赖与浏览器
        run: |
          npm ci --prefix web/admin-console
          npm ci --prefix web/user-console

      - name: 安装 Chromium
        working-directory: web/admin-console
        run: npx playwright install --with-deps chrome

      - name: 执行无 Mock 的真实后端写入与调用链路
        env:
          AI_GATEWAY_G8_ISOLATED_APPROVED: YES
        run: |
          bash -n infra/scripts/verify-ai-gateway-g8-real-backend-e2e.sh
          bash infra/scripts/verify-ai-gateway-g8-real-backend-e2e.sh

  # ---- 前端 CI ----
  frontend-admin:
    name: 管理后台构建
    needs: change-scope
    if: needs.change-scope.outputs.frontend_admin == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: 安装依赖
        working-directory: web/admin-console
        run: npm install

      - name: 管理后台阶段 4 契约测试
        working-directory: web/admin-console
        run: |
          npm run test:sms-management
          npm run test:admin-verification
          npm run test:email-management
          npm run test:vite-isolation
          npm run test:outbound-url

      - name: TypeScript 类型检查
        working-directory: web/admin-console
        run: npm run type-check

      - name: ESLint 检查
        working-directory: web/admin-console
        run: npm run lint

      - name: 生产构建
        working-directory: web/admin-console
        run: npm run build

  frontend-user:
    name: 用户控制台构建
    needs: change-scope
    if: needs.change-scope.outputs.frontend_user == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: 安装依赖
        working-directory: web/user-console
        run: npm install

      - name: 用户控制台阶段 4 契约测试
        working-directory: web/user-console
        run: |
          npm run test:sms
          npm run test:email-otp
          npm run test:vite-isolation

      - name: TypeScript 类型检查
        working-directory: web/user-console
        run: npm run type-check

      - name: ESLint 检查
        working-directory: web/user-console
        run: npm run lint

      - name: 生产构建
        working-directory: web/user-console
        run: npm run build

      - name: G6 用户完整链路浏览器验收
        working-directory: web/user-console
        run: |
          npx playwright install --with-deps chrome
          npm run test:g6-e2e

  ci-required-gate:
    name: CI 必选门禁汇总
    needs:
      - change-scope
      - docs-lightweight
      - backend
      - phase5-release-safety
      - gateway-g3
      - gateway-g4
      - gateway-g7
      - gateway-g8
      - gateway-g8-real-e2e
      - frontend-admin
      - frontend-user
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: 校验分类结果与适用门禁
        env:
          CHANGE_SCOPE_RESULT: ${{ needs.change-scope.result }}
          FULL_REQUIRED: ${{ needs.change-scope.outputs.full }}
          DOCS_REQUIRED: ${{ needs.change-scope.outputs.docs_lightweight }}
          DOCS_RESULT: ${{ needs.docs-lightweight.result }}
          BACKEND_REQUIRED: ${{ needs.change-scope.outputs.backend }}
          BACKEND_RESULT: ${{ needs.backend.result }}
          PHASE5_REQUIRED: ${{ needs.change-scope.outputs.phase5 }}
          PHASE5_RESULT: ${{ needs.phase5-release-safety.result }}
          G3_REQUIRED: ${{ needs.change-scope.outputs.gateway_g3 }}
          G3_RESULT: ${{ needs.gateway-g3.result }}
          G4_REQUIRED: ${{ needs.change-scope.outputs.gateway_g4 }}
          G4_RESULT: ${{ needs.gateway-g4.result }}
          G7_REQUIRED: ${{ needs.change-scope.outputs.gateway_g7 }}
          G7_RESULT: ${{ needs.gateway-g7.result }}
          G8_REQUIRED: ${{ needs.change-scope.outputs.gateway_g8 }}
          G8_RESULT: ${{ needs.gateway-g8.result }}
          G8_E2E_REQUIRED: ${{ needs.change-scope.outputs.gateway_g8_real_e2e }}
          G8_E2E_RESULT: ${{ needs.gateway-g8-real-e2e.result }}
          ADMIN_REQUIRED: ${{ needs.change-scope.outputs.frontend_admin }}
          ADMIN_RESULT: ${{ needs.frontend-admin.result }}
          USER_REQUIRED: ${{ needs.change-scope.outputs.frontend_user }}
          USER_RESULT: ${{ needs.frontend-user.result }}
        run: |
          test "$CHANGE_SCOPE_RESULT" = "success"
          for value in \
            "$FULL_REQUIRED" "$DOCS_REQUIRED" "$BACKEND_REQUIRED" "$PHASE5_REQUIRED" \
            "$G3_REQUIRED" "$G4_REQUIRED" "$G7_REQUIRED" "$G8_REQUIRED" \
            "$G8_E2E_REQUIRED" "$ADMIN_REQUIRED" "$USER_REQUIRED"; do
            case "$value" in
              true|false) ;;
              *) printf 'CI 分类输出不是布尔值\n'; exit 1 ;;
            esac
          done
          selected_gate=false
          for value in \
            "$DOCS_REQUIRED" "$BACKEND_REQUIRED" "$PHASE5_REQUIRED" "$G3_REQUIRED" \
            "$G4_REQUIRED" "$G7_REQUIRED" "$G8_REQUIRED" "$G8_E2E_REQUIRED" \
            "$ADMIN_REQUIRED" "$USER_REQUIRED"; do
            if [ "$value" = "true" ]; then
              selected_gate=true
            fi
          done
          test "$selected_gate" = "true"
          if [ "$FULL_REQUIRED" = "true" ]; then
            test "$DOCS_REQUIRED" = "true"
            test "$BACKEND_REQUIRED" = "true"
            test "$PHASE5_REQUIRED" = "true"
            test "$G3_REQUIRED" = "true"
            test "$G4_REQUIRED" = "true"
            test "$G7_REQUIRED" = "true"
            test "$G8_REQUIRED" = "true"
            test "$G8_E2E_REQUIRED" = "true"
            test "$ADMIN_REQUIRED" = "true"
            test "$USER_REQUIRED" = "true"
          fi
          check_required_gate() {
            local required="$1"
            local result="$2"
            local gate_name="$3"
            if [ "$required" = "true" ] && [ "$result" != "success" ]; then
              printf '必选 CI 门禁未成功：%s result=%s\n' "$gate_name" "$result"
              return 1
            fi
          }
          check_required_gate "$DOCS_REQUIRED" "$DOCS_RESULT" "docs-lightweight"
          check_required_gate "$BACKEND_REQUIRED" "$BACKEND_RESULT" "backend"
          check_required_gate "$PHASE5_REQUIRED" "$PHASE5_RESULT" "phase5-release-safety"
          check_required_gate "$G3_REQUIRED" "$G3_RESULT" "gateway-g3"
          check_required_gate "$G4_REQUIRED" "$G4_RESULT" "gateway-g4"
          check_required_gate "$G7_REQUIRED" "$G7_RESULT" "gateway-g7"
          check_required_gate "$G8_REQUIRED" "$G8_RESULT" "gateway-g8"
          check_required_gate "$G8_E2E_REQUIRED" "$G8_E2E_RESULT" "gateway-g8-real-e2e"
          check_required_gate "$ADMIN_REQUIRED" "$ADMIN_RESULT" "frontend-admin"
          check_required_gate "$USER_REQUIRED" "$USER_RESULT" "frontend-user"
