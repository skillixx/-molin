# 仪表盘由版本库只读 Provision，测试环境 UI 不允许直接改写形成配置漂移。
apiVersion: 1

providers:
  - name: Molin G7
    orgId: 1
    folder: 墨灵 Token 网关
    type: file
    disableDeletion: true
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
