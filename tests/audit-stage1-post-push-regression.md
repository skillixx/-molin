# 第一阶段 — 推送后回归确认测试（Post-Push Regression）

**测试时间**：2026-06-08
**测试环境**：测试服务器 `8.130.9.163:8080`（API + MySQL 13306）
**测试目的**：验证代码 push 到 `origin/main` 之后，线上代码状态依然稳定，未在合并/推送过程中引入任何问题（"最后的安全网检查"）

## 关键提交确认

执行 `git fetch origin && git log --oneline origin/main` 确认以下关键提交均已在 `origin/main` 上：

| commit | 说明 |
|---|---|
| `32645e0` | auth：补充管理员封禁/解封用户接口（PATCH /api/admin/users/:id/status）|
| `d921949` | auth：补充 user:manage 权限码种子数据（migration 000012）|
| `d5115c8` | 合并：补充 user:manage 权限码种子数据修复 PR（migration 000012）|
| `0cfd9eb` | 合并：第一阶段全部收尾完成，更新 README（HEAD）|

本地 `git checkout main && git pull origin main` 显示"已经是最新的"，本地分支与 `origin/main` 完全一致。

## 测试一：端到端全链路回归测试（`tests/stage1_e2e_test.py`，37 个用例）

执行方式：上传脚本到测试服务器，远程执行（`API_BASE=http://localhost:8080`，直连本机 MySQL 13306）。

### 结果：**37/37 全部通过（100%）**

覆盖范围（与之前一致）：

- 准备：创建/登录管理员账号（自动注册 + 绑定 admin 角色）
- 用例 1-4：邮箱/手机号注册 + 登录、重复邮箱/手机号拦截（HTTP 409）
- 用例 5：用户注册后提交实名认证 + 查询认证状态
- 用例 6：未实名用户不能购买商品 → 400 / code=70001
- 用例 9/12/13/14：余额不足拦截（400/60001）、充值、普通购买成功、钱包扣费、流水生成、资产生成
- 缺陷复现回归：全新用户首次购买（钱包懒创建链路）→ 正确返回 400/60001，无 500 异常（确认此前 P1 缺陷修复依然有效）
- 用例 10/11：VIP 会员价购买（按会员价 6.00 正确扣费）/ 非会员购买会员专属应用被拦截（403/40003）
- 用例 15/16：管理员修改用户角色后权限立即生效（无需重新登录）/ 角色撤销后用户立即无法访问应用
- 旁路验证：资产到期时间字段、权益记录

测试输出汇总行：

```
总计: 37  通过: 37  失败: 0
```

**与此前 37/37 验收结果完全一致，无回归、无新增异常。**

## 测试二：封禁/解封接口抽查（`PATCH /api/admin/users/:id/status`）

按用户要求，使用**全新自包含测试账号**（`qa_ban_admin_{timestamp}@molin.io` / `qa_ban_target_{timestamp}@molin.io` / `qa_ban_normal_{timestamp}@molin.io`，固定密码 `Test1234!`），通过正常注册流程创建，仅对操作者账号执行 `INSERT IGNORE INTO user_roles (user_id, role_id) VALUES (?, 1)` 绑定系统已存在的真实 `admin` 角色（不新建角色、不手动播种权限码）。

### 结果：**12/12 全部通过**

| # | 用例 | 期望 | 实际 | 结论 |
|---|---|---|---|---|
| 1 | 三个全新自包含测试账号注册成功 | 成功 | 成功（uid=72/73/74）| 通过 |
| 2 | admin / target / normal 登录获取 token | 成功 | 成功 | 通过 |
| 3 | admin 角色账号调用封禁接口封禁 target | 200 | 200 `{"code":0,"data":"updated"}` | 通过 |
| 4 | 被封禁用户旧 access_token 访问 `/api/me` | 401 | 401 `{"code":40101,"message":"账号已被封禁"}` | 通过 |
| 5 | 被封禁用户旧 refresh_token 刷新 | 401 | 401 `{"code":40001,"message":"凭证无效或已过期"}` | 通过 |
| 6 | admin 解封 target 账号 | 200 | 200 `{"code":0,"data":"updated"}` | 通过 |
| 7 | 解封后用户可重新登录获取新 token | 成功 | 成功 | 通过 |
| 8 | 解封后新 token 访问 `/api/me`，status=active | 200 / active | 200 / `status:"active"` | 通过 |
| 9 | 非 admin 账号（normal）调用封禁接口 | 403 | 403 `{"code":40003,"message":"无操作权限"}` | 通过 |
| 10 | 无 Token 调用封禁接口 | 401 | 401 `{"code":40001,"message":"未登录"}` | 通过 |

封禁立即生效（access token 与 refresh token 同时失效）、解封后正常恢复、权限边界（403/401）均符合预期，**与此前 `audit-stage1-closing-confirm.md` 的验证结论一致**。

测试用临时脚本（`ban_check.py`）已在测试服务器上清理，未留存任何残留文件；测试数据为全新账号，未修改/覆盖任何已有数据。

## 安全约束遵守说明

- 全程使用自包含测试账号（带时间戳邮箱 + 固定密码 `Test1234!`），通过正常注册流程创建
- 仅对自己创建的全新账号执行 `INSERT IGNORE` 绑定系统已存在的 `admin` 角色，未新建角色/权限码，未覆盖已有数据
- 未尝试登录任何文档中提到的已存在管理员或测试账号

## 结论

| 项目 | 结果 |
|---|---|
| 关键提交是否都在 origin/main | 是（32645e0 / d921949 / d5115c8 / 0cfd9eb 均确认存在）|
| 端到端全链路回归（37 用例）| **37/37 通过（100%）**，与此前验收结果一致 |
| 封禁/解封接口抽查（12 用例）| **12/12 通过**，与此前收尾验证结果一致 |
| 是否发现新问题 | **否，未发现任何新问题或回归** |

**推送后回归确认结论：代码状态稳定，与本地验收结果完全一致，"最后的安全网检查"通过，无需任何额外动作。**
