# 第一阶段端到端确认性复测报告（最终合并状态确认）

**测试时间**：2026-06-07
**测试分支**：feature/test-stage1-final-confirm
**测试依据 commit**：origin/main @ `9254eee`（已确认本地与远程一致，且该 commit 仅新增测试报告/脚本，未改动 `server/` 任何代码；最后一次影响 `server/` 的 commit 为 `131613e` —— billing 钱包懒创建修复合并）
**测试服务器**：8.130.9.163:10003，API 端口 8080，MySQL 13306
**测试脚本**：`tests/stage1_e2e_test.py`（沿用既有脚本，未做任何修改）

## 一、部署确认

- 检查发现测试服务器上没有部署 git 仓库（API 以预编译二进制方式运行），本地与远程沙箱均无 Go 工具链，无法重新编译。
- 经核对部署的二进制文件 `~/molin/molin-api` 构建/上传时间为 **2026-06-07 21:17**，晚于最后一次代码改动 commit `131613e`（**2026-06-07 20:32**），早于纯文档合并 commit `9254eee`（**2026-06-07 21:51`，该 commit 对 `server/` 无任何 diff）。
- 结论：当前测试服务器运行的二进制已包含缺陷 #1 的全部修复代码（`9fe6bef` + `131613e`），与 `origin/main@9254eee` 的 `server/` 代码状态完全一致，无需也无法重新构建。
- 已重启 API 进程（`pkill` + `nohup` 重新拉起），确保是全新干净的运行时状态，避免历史进程状态干扰复测结果。重启后 `/api/health` 返回 `{"code":0,"message":"ok"}`。

## 二、复测执行

按相同环境变量、相同脚本，**连续执行两轮**端到端测试（用于核查稳定性/flaky 现象）：

| 轮次 | 总用例 | 通过 | 失败 | 通过率 |
|---|---|---|---|---|
| 第 1 轮 | 37 | 36 | 1 | 97.3% |
| 第 2 轮 | 37 | 36 | 1 | 97.3% |

两轮结果**完全一致**，无任何用例结果在两轮间发生翻转，未观察到不稳定（flaky）现象。

## 三、缺陷 #1 复现用例核查（重点）

用例：「全新用户首次购买（从未访问钱包，未充值）」

- 第 1 轮：`PASS` —— 全新用户首次购买在余额不足场景下正确返回 `HTTP 400 {"code":60001}`，钱包懒创建链路无异常
- 第 2 轮：`PASS` —— 结果一致

两轮均稳定复现修复后的预期行为（不再触发 HTTP 500），**缺陷 #1 在最终合并状态下修复有效，未发现回归**。

同时检查了测试期间的 API 运行日志 `~/molin/api.log`，未发现任何 `panic`/`500`/`error` 级别异常输出。

## 四、缺陷 #2（已知问题，非回归）

用例：「非会员无法购买会员专属应用」

- 两轮均为 `FAIL`：非会员账号仍可成功下单（HTTP 200），系统当前购买访问控制仅基于角色 `product_role_access.can_buy`，未实现与 `membership_level` 绑定的"会员专属商品禁止非会员购买"业务规则。
- 此为已知业务规则缺失问题（产品经理已知悉，非本轮新发现，非代码回归），与上一轮复测结果一致。

## 五、结论

1. **远程 main（`origin/main@9254eee`）代码状态稳定**：该 commit 仅新增测试文档与脚本，未对 `server/` 代码产生任何变更；测试服务器上运行的二进制构建时间晚于最后一次代码修改 commit（`131613e`），与远程 main 的 server 代码状态完全一致。
2. **缺陷 #1 修复在合并后的最终状态下依然有效**：连续两轮复测均稳定返回 `HTTP 400 {"code":60001}`，未再触发 500，合并过程未引入意外问题。
3. **整体通过率与上一轮完全一致**：36/37 = 97.3%，两轮复测结果完全相同，**未发现新的不稳定（flaky）现象或新增回归**。
4. **缺陷 #2 仍为预期中的已知问题**（业务规则未实现），无需重复分析，结果与历史一致。

**总体结论：通过（确认性复测稳定，无新问题，可继续推进流程）。**
